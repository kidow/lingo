# Lingo — 명세서

시각 중심 언어 학습 서비스. v1은 일본어 단어 학습.

이 문서는 **v1에서 무엇을 만들고 무엇을 만들지 않는지**를 확정한다.
이미지 스타일 규칙은 [IMAGE_STYLE.md](IMAGE_STYLE.md), 발음 규칙은 [AUDIO.md](AUDIO.md),
시각 규칙은 [brand-spec.md](brand-spec.md)에 있다.

---

## 1. 제품 정의

단어에 AI 생성 이미지를 매칭하고, **끝이 없는 세로 피드를 훑으면서** 의미를 기억시키는 학습 서비스.

숏폼을 빠르게 넘기는 습관을 학습 메커니즘으로 쓴다. 그래서 이 서비스에는
**세션도, 오늘의 목표도, 결과 화면도 없다.** 열면 카드가 떠 있고, 위로 밀면 다음 카드가 온다.

이미지는 장식이 아니라 학습 요소이며, 이미지 스타일 자체를 디자인 시스템의 일부로 관리한다.

최종 지향은 단어장이 아니라 **단어를 허브로 문법·회화·예문이 연결되는 학습 시스템**이다.
다만 v1은 피드 하나의 완성도만 다룬다.

### 핵심 구조 1: 개념(concept)이 중심이다

이 서비스의 축은 단어가 아니라 **개념**이다.

> 바나나 그림은 하나면 된다. 일본어로 배우든 독일어로 배우든 같은 그림이다.
> 언어마다 달라지는 것은 표기·읽기·발음뿐이다.

| 개념에 속하는 것 | 단어에 속하는 것 |
|---|---|
| 이미지 | 표기 (`バナナ`, `Banane`) |
| 한국어 뜻 | 읽기 |
| 이미지 프롬프트 | 로마자 |
| 카테고리 | 품사 · 언어별 문법 속성 |
| | 발음 오디오 |

수동 이미지 파이프라인이 이 서비스의 유일한 병목이다. 개념을 분리하면
8개 언어 × 60단어를 이미지 60장으로 감당한다. 언어 추가는 **표기 몇 줄을 더 쓰는 일**이 된다.

**다만 언어마다 같은 단어를 배우지는 않는다.** `words`가 부분 맵인 것이 그 여지다 —
개념에 그 언어의 단어가 없으면 그 언어에서는 출제되지 않는다. 일본어 입문 어휘로 맞는
`バナナ`가 영어에서는 너무 쉬워서, 영어는 `anchor` · `crutch` 처럼 덜 흔한 구체명사로
따로 간다. 겹치는 개념이 생기면 그때 이미지를 공유하고, 겹치지 않으면 각자 만든다.

그래도 **그릴 수 있어야 한다**는 제약은 언어와 무관하게 같다. 난이도를 올리려고
추상어로 가면 카드 5종이 전제하는 이미지가 성립하지 않는다.

### 핵심 구조 2: 학습 대상은 언어마다 하나로 고정한다

일본어 단어에는 표시할 텍스트가 둘이다 — 표기 `猫`와 읽기 `ねこ`.
**둘 중 하나만 정답으로 쓴다.** v1 일본어는 **읽기(かな)** 다.

**영어는 표기가 하나다.** `apple` 하나뿐이라 일본어처럼 두 형태로 갈라지지 않는다.
대신 참고줄에 **발음기호(IPA)** 를 둔다 — 영어는 적힌 대로 읽지 않는 언어다.
`receipt`도 `colonel`도 그렇다. CMU 사전에서 받아 `pnpm ipa`가 채운다 (§7).

기호는 **학습자 사전 표기**를 쓴다. 옥스퍼드·롱맨이 `r`로 싣는 것을 음성학
기호 `ɹ`로 적지 않는다 — 카드를 보는 사람은 음성학자가 아니다. 사전에 없는
낱말은 비운다. 소리는 그래도 발음 버튼이 준다.

근거:

- 한자를 모르는 초급자에게 `猫`를 보여주고 `ねこ`를 묻는 것은 순환 논리다.
  한자를 알아야 풀 수 있는데 그 한자를 아직 안 배웠다.
- 학습 순서 권장안은 어디서든 같다 — **가나 완성 → 기초 어휘 → 한자는 그 다음, 전용 도구로.**
- Duolingo 일본어도 초급 구간을 가나로 출제한다. 이유를 둘로 밝힌다 —
  한자 수백 개로 압도하지 않기 위해서, JLPT도 모든 어휘의 한자를 요구하지 않기 때문에.
- 그 Duolingo가 **정확히 이 지점에서 가장 많이 비판받는다**: "같은 단어가 어떤 레슨에선 한자로,
  다른 레슨에선 히라가나로 나오는데 규칙이 없다." 가나냐 한자냐보다 **하나를 정해 일관되게 지키는 것**이
  더 중요하다.
- 로마자는 "처음엔 필요악이지만 금세 심각한 장애물"이 된다는 게 중평이다.
  `romanization`은 소개 카드의 참고 표시로만 쓰고 **보기·정답에는 절대 쓰지 않는다.**

한자는 소개 카드에서 작게 참고로만 보여준다. 한자 학습은 별도 트랙으로 나중에 다룬다.

언어마다 이 선택이 달라질 수 있으므로 **하드코딩하지 않고 설정표로 둔다** (§4).

### 트랙

사용자가 고르는 단위는 언어가 아니라 **시험**이다. 무엇을 공부하는지가
"일본어"보다 "JLPT"에 더 가깝다.

| 트랙 | 언어 | 레벨 |
|---|---|---|
| TOEIC | 영어 | `TSL 1`~`TSL 1250` — 등급이 아니라 빈도 순위다 |
| JLPT | 일본어 | `N5`~`N1` |
| HSK | 중국어 | `1`~`6` · `7-9` (7~9급은 한 묶음이다) |
| TOCFL | 중국어(번체) | `準備1`·`準備2`·`L1`~`L5` |
| DELE | 스페인어 | **비어 있다** — 낱말별 등급을 담은 공개 목록이 없다 |
| DELF | 프랑스어 | **비어 있다** — FLELex는 등급이 아니라 등급별 빈도 분포다 |
| TELC | 독일어 | CEFR `A1`~`B1` (Goethe 목록이 B1까지다) |
| TORFL | 러시아어 | CEFR `A1`~`B2` (ros-edu.ru의 ТРКИ 최소치가 B2까지다) |

여덟 트랙 가운데 **여섯이 콘텐츠 전량을 덮는다.** TOEIC은 TSL 필터를, TOCFL은
八千詞表 필터를 한 겹 더 거치므로 분모가 작다 (§4).

**HSK와 TOCFL은 트랙 둘이 `zh` 하나를 나눠 쓰는 첫 사례다.** 언어는 하나인데
카드에 낼 표기가 갈린다 — HSK는 간체(`term`)가 정답이고, TOCFL은 대만
시험이라 번체(`traditional`)가 정답이다. `lib/lang.ts`의 `TRACK_OVERRIDE`가
트랙별로 `LangStrategy`(정답 필드·참고줄)를 덮어쓴다. TOCFL 카드의 참고줄은
병음을 그대로 두고 간체를 곁들인다 — 학습자는 한국 사용자이지 대만
원어민이 아니라서, 병음을 빼면 소리 없이는 발음을 알 길이 없다. 예문·발음은
그대로 공유한다 — 간체든 번체든 읽는 소리가 같아서다(scripts/tocfl.ts).

레벨이 채워지는 정도는 트랙마다 다르다. **숫자는 `pnpm coverage`가 낸다** —
배치를 한 번 돌 때마다 낡으므로 여기 박지 않는다 (§7). DELE·DELF만 0%다.
**UI는 트랙을 가리지 않는다**(같은 코드가 `levelOf`를 읽는다). 차이는
데이터이고, 채울 수 없는 것은 비운다.

스페인어·프랑스어에는 한때 초기 51개 개념에만 손으로 넣은 `A1`이 남아 있었다.
2%만 등급이 보이면 나머지가 "등급 없는 낱말"처럼 읽히므로 걷어냈다 —
**출처에서 온 것만 붙인다는 규칙이 손으로 넣은 값에도 적용된다.**

**TORFL은 B2까지만 붙는다.** 오래 비어 있던 트랙이다 — ТРКИ의 «Лексический
минимум»은 Златоуст에서 나온 책이라 PDF조차 없었다. ros-edu.ru가 그 최소치를
A1~B2 네 등급으로 웹에 싣고 목록 화면이 그리는 JSON을 그대로 내주어 채웠다
(`scripts/torfl.ts`). C1·C2는 그 사이트에도 없으므로 여전히 비어 있고, 그
낱말은 카드 좌하단이 자리째 빠진다 (§5).

**러시아어는 참고줄에 로마자를 둔다.** 키릴은 한글·라틴 어느 쪽도 아니라 표기를
그대로 읽을 수 없다. 다만 글자와 소리가 거의 일대일이라 사전 없이 표 하나로
만들 수 있다 — `pnpm romanize`가 낱말과 예문을 함께 채운다 (§7).

레벨은 트랙을 쪼개지 않고 **카드에 표시만** 한다 (§5). 예외가 둘 있다 —
**TOEIC과 TOCFL은 출제 대상을 거른다.** ETS는 공식 어휘 목록을 내지
않으므로 TOEIC Service List(§7)에 있는 단어만 낸다. `cat`·`rice`는 JLPT
N5·HSK 1급으로는 제값을 하지만 TOEIC 시험에는 나오지 않는다. TOCFL도
같은 이유로 八千詞表에 있는 낱말만 낸다 — 그림이 있다고 다 시험에 나오는
건 아니다. 개념을 여덟 트랙이 공유하는 구조의 대가이고, 개념을 지우는 게
아니라 그 트랙에서만 빼는 것이다. 규칙은 `lib/entries.ts`의
`entriesForTrack` 하나에 있다.

**트랙은 언어를 대체하지 않고 위에 얹힌다.** 그리고 **개념은 트랙이 공유한다** —
`cat` 그림 한 장을 여덟 트랙이 같이 쓴다. 어느 트랙에 나올지는 그 개념이 그 언어의
단어를 갖고 있느냐로 정해진다. 트랙을 더해도 **그려야 할 그림은 늘지 않는다.**
발음도 시험이 아니라 언어의 것이라 `public/audio/{language}/`를 공유한다 — HSK와
TOCFL처럼 트랙 둘이 언어 하나를 나눠 써도 마찬가지다.

- **v1 콘텐츠**: 여덟 트랙 (TOEIC · JLPT · HSK · TOCFL · DELE · DELF · TELC · TORFL)
- **최종 목표**: 위 일곱 언어에 아랍어
- 스키마와 라우팅은 처음부터 언어 중립이다.

### 사용자

- v1은 **로그인 없음**. 익명 사용.
- 학습 진도와 학습 언어 설정은 브라우저 `localStorage`에만 저장한다.
- **서버가 없다.** 콘텐츠는 레포에 정적으로 들어가고 빌드 시점에 번들된다 (§4).

---

## 2. 비범위 (v1에서 만들지 않는 것)

| 항목 | 이유 |
|---|---|
| 단어장 화면 · 설정 화면 | MVP는 피드 하나다. 탭 자체가 없다 |
| 홈 · 세션 · 결과 화면 | 피드에는 시작도 끝도 없다 |
| 데스크톱 레이아웃 | 모바일 전용이다. `481px` 이상은 안내 카드 한 장으로 끝낸다 (§3) |
| 덱 (`decks`, `deck_concepts`) | 피드가 전체 단어를 섞는다. 고를 게 없다 |
| Leitner 5상자 | 날짜 기반 상자 스케줄을 피드 모델로 대체 (§6) |
| 자가판정 ("알아요 / 몰라요") | 객관적으로 채점되는 카드만 남긴다 |
| 데이터베이스 (Supabase 등) | 콘텐츠가 레포 안에 있고 진도는 localStorage다. 서버가 할 일이 없다 (§4) |
| 인증 / 계정 / 서버 진도 동기화 | 익명 + localStorage로 시작 |
| 관리자 웹 UI | 데이터 저작은 레포 내 JSON 파일을 손으로 고쳐서 한다 |
| 단어 목록의 출처를 교재에서 복제 | 레포가 공개다. 목록은 직접 만들고 `pnpm define`으로 뜻을 대조한다 (§7) |
| 한자 학습 트랙 | 학습 대상은 읽기 하나로 고정 (§1) |
| 문법 · 회화 | 단어가 허브가 되는 구조는 v1 다음이다 |
| 예문 발음 **파일** | 이름 규칙과 버튼 자리는 정해 뒀다(§5). 파일은 아직 0개라 버튼이 안 뜬다 |
| `word_senses` / 다의어 정규화 | **뜻이 갈리면 개념을 나눈다.** `gum`(껌)과 `gums`(잇몸)이 이미 그렇게 서 있다 — 그림이 다르므로 한 개념에 담을 수 없다. `bank`가 은행 하나뿐인 것은 뜻을 하나만 적어서가 아니라 **둑이라는 개념을 아직 안 만들어서**다. 뜻이 같고 말만 여럿인 자리는 `Word.also`로 곁에 적는다 (§5) |
| 개념당 이미지 여러 장 | 개념당 이미지 1장 |
| 스트릭 · 일일 목표 · 배지 | 목표가 없는 피드에는 붙일 자리가 없다. **헤더 숙련도(§3)는 예외** — 목표가 아니라 상태다 |
| 서비스워커 / 오프라인 학습 | PWA는 manifest + 아이콘까지만 |
| 다크모드 | warm paper 라이트 단일 |
| 앱 내 이미지 생성(OpenAI Images API) | 이미지는 수동 파이프라인 |
| TanStack Query | 서버 컴포넌트 + 정적 생성으로 충분 |

---

## 3. 화면

**화면은 하나다.**

```
/            피드
```

```
┌─────────────────┐
│ JLPT ⌄ 12% 단어│표현│상식│  헤더 — 트랙 · 숙련도 · 덱 탭
├─────────────────┤
│                 │
│      카드        │  남는 높이를 전부 쓴다
│                 │
└─────────────────┘
```

하단 탭도, 뒤로가기도, 메뉴도 없다. 갈 곳이 없기 때문이다.

**헤더에는 학습 트랙과 숙련도, 덱 탭만 둔다.** 오랫동안 헤더를 두지 않았던 이유는 화면이 하나뿐이라
이동할 곳이 없어서였다. 트랙이 둘이 되면서 **고를 것**이 생겼고, 고를 것은 카드 안에
둘 수 없다 — 카드는 넘기는 물건이라 설정이 섞이면 오터치가 난다.

- 왼쪽에 드롭다운 하나. `TOEIC` / `JLPT`가 그대로 텍스트다.
- shadcn/ui `dropdown-menu`(Base UI 기반)를 쓴다. 항목은 **라디오**다 — 고르는 행위이므로
  지금 무엇이 켜져 있는지가 체크 표시로 드러나고 스크린리더에도 그렇게 읽힌다.
  포커스 관리·Esc·바깥 클릭을 직접 짜지 않는다. (§8)
- 트랙을 바꾸면 피드를 통째로 새로 만든다. **진도는 트랙별로 갈라져 있으므로**(§6)
  서로를 덮지 않고, 돌아오면 하던 자리에서 이어진다.
- 선택은 `localStorage`에 남는다. 서버는 어느 트랙인지 모른다.
- 그림이 있는 단어가 하나도 없는 트랙은 빈 화면을 보여준다. 목록이 비었다는 뜻이
  아니라 **아직 그리지 않았다**는 뜻이다 (§4).

**오른쪽 끝에 덱 탭 — `단어 | 표현 | 상식`.** 카드에는 낱말과 통짜 표현이 섞여 있는데
(`계산해 주세요`) 외우는 방식이 다르다 — 낱말은 뜻을 붙이는 일이고 표현은 상황에
통째로 붙이는 일이다. 그래서 보는 자리를 나눈다 (`lib/deck.ts`).

- 드롭다운이 아니라 **늘 보이는 두 글자**다. 고를 것이 두셋뿐이고, 지금 무엇을 보는지가
  트랙만큼 자주 바뀐다.
- **단어와 표현은 진도를 나누지 않는다.** 덱은 걸러 보는 창이고 `lingo.progress.{track}`
  하나를 그대로 쓴다. 나누면 표현 380개짜리 진도가 며칠이면 바닥나고 헤더 숙련도가
  무엇의 비율인지 흐려진다. 둘의 숙련도 분모는 덱과 무관하게 **트랙 전체**다.
- 가르는 기준은 `category`다 — `scene`이 표현, 나머지 셋이 단어다 (§4).
- **상식은 세 번째 덱이다.** 그 언어에 대한 사실을 묻는 4지선다이고, 낱말이 아니라
  언어 자체를 다룬다 (§4의 `Trivia`). 저장소는 같이 쓰되 키 앞자리가 다르고
  (`trivia:{lang}:{id}`), **숙련도는 합치지 않는다** — 세는 단위가 달라 한 줄에
  못 합친다. 상식 탭을 볼 때는 헤더 숫자도 상식 안에서의 비율이 된다.
- **고를 것이 하나뿐인 트랙에서는 탭이 서지 않는다.** TOEIC은 TSL 표제어만 내는데 통짜
  표현은 그 목록에 없다. 저장된 값이 그 트랙에 없는 덱이면 단어로 되돌려 본다 —
  탭이 안 서는데 거르기만 남으면 빈 피드가 된다.
- 덱은 트랙과 달리 **하나만** 저장한다(`lingo.deck`). 무엇을 공부할지는 언어가 아니라
  사람에게 달린 선택이라 트랙을 오가도 따라간다.

**드롭다운 오른쪽에 숙련도 하나를 더 둔다.**

```
┌─────────────────┐
│ JLPT ⌄  12%      │  트랙 + 완전히 외운 비율
├─────────────────┤
```

- **목표가 아니라 상태다.** 하루가 지나도 리셋되지 않고, 채워도 축하하지 않으며,
  채우라고 조르지 않는다. 이 성질 때문에 §2가 배제한 스트릭·일일 목표와 다르다.
- 분자는 **완전히 외운 개수**(§6), 분모는 **그 트랙에서 출제 가능한 개념 수**다.
  TOEIC은 TSL 필터를 한 겹 더 거치므로(§4) 분모가 다른 트랙보다 작다.
- **0개면 아무것도 쓰지 않는다.** 설치 직후 `0%`가 며칠씩 박혀 있으면 고장으로 읽힌다.
  반올림해서 0이 되는 구간은 `<1%`로 쓴다 — JLPT는 분모가 커서 열몇 개를 외워야
  1%가 되는데 그 사이를 `0%`로 쓰면 한 일이 없다고 말하는 셈이다.
- **되돌아간다.** 외웠던 단어를 틀리면 빠져서 숫자가 내려간다. 현재 상태를 뜻하려면
  그래야 한다.
- 드롭다운 **트리거 밖**에 둔다. 안에 넣으면 버튼 이름이 `학습 트랙 12%`가 된다.

### 피드

세로 무한 캐러셀. 한 장이 화면 하나를 꽉 채운다. 위로 밀면 다음 카드.

카드 안 구성은 세 종류 모두 같은 골격이다.

```
┌─────────────────┐
│                 │
│     이미지       │  정사각. 레일 폭을 꽉 채운다. 높이는 52dvh까지
│                 │
├─────────────────┤
│  카드별 본문 시트  │  이미지 위로 올라타 둘이 한 덩어리로 읽힌다
│                 │  --surface 면, 위쪽 모서리만 둥글다. 아래 §5
└─────────────────┘
```

이미지에 여백을 두지 않는다. 모바일 전용이므로 가운데 띄울 이유가 없고,
시트가 이미지를 물고 올라와 **이미지와 글자가 한 장으로 보인다.** 시트에
테두리는 없다 — 선을 그으면 그림이 사각형에 갇힌다.

**스와이프 규칙**

- **소개 카드** — 자유롭게 넘긴다. 넘기는 순간 학습한 것으로 인정한다.
- **퀴즈 카드** — **답하기 전에는 잠긴다.** 스킵할 수 없다.
- 정답이든 오답이든 **자동 진행은 없다.** 항상 사용자가 직접 민다.
- **안내 문구를 쓰지 않는다.** `답을 고르세요` 같은 지시문 대신 시트 맨 아래
  위쪽 겹화살표 아이콘(lucide `chevrons-up`) 하나가 위로 튄다.
  말하려는 것은 "무엇을 하라"가 아니라 **"위로 갈 수 있다"** 다.
- 화살표가 뜨는 조건은 카드마다 다르다. **소개 카드는 상시**, **퀴즈 카드는
  답한 뒤**다. 화살표가 보이는 것과 실제로 넘어갈 수 있는 것이 항상 같다 —
  잠금은 여전히 구조가 건다. 미응답 퀴즈 다음 카드는 아예 만들어지지 않는다.
- 화살표는 스크린리더에서 숨긴다. 스크롤은 원래 되는 것이고 아이콘은 그
  사실을 눈으로만 알려 준다. 움직임을 줄이는 설정에서는 애니메이션이 멈춘다.

**화면 너비 — 480px 경계**

Lingo는 **모바일 전용이다.** 뷰포트 너비가 `480px` 이하일 때만 피드를 그린다.

- `481px` 이상에서는 피드 대신 **안내 카드** 한 장을 띄운다.
  제목 `화면이 너무 넓어요` · 설명 `휴대폰에서 열거나 창을 좁혀 주세요`.
- 안내 카드는 **카드와 같은 골격**이다 — 풀블리드 이미지(폰 아이콘) 위로 본문
  시트가 올라타고, 시트 안에 제목과 설명이 들어간다.
  안내라고 다른 모양을 만들면 화면이 둘이 된다. 이 제품에 있는 것은 카드뿐이다.
- 문구는 기기를 지칭하지 않고 **화면 상태만** 말한다. 판정이 너비 하나뿐이라
  가로로 눕힌 폰(`844×390`)도 이 화면을 보기 때문이다.
- 포인터도 UA도 보지 않는다. 규칙이 CSS 두 줄로 끝나고, 정적 빌드 결과가
  기기를 몰라도 되며, 하이드레이션 깜빡임이 원천적으로 없다.
- 데스크톱에서 창을 좁히면 그 자리에서 피드가 살아난다. 피드는 숨어 있을 뿐
  마운트되어 있다 — 진도는 `progress`를 건드리지 않는 경로만 돌아 오염되지 않는다.

**발음 버튼**

- **정답 텍스트 오른쪽**에 선다. 소개 카드는 읽기 옆, 퀴즈 카드는 답한 뒤 드러나는
  Reveal 줄 옆이다. 이미지 위에 얹지 않는다 — 그림을 가리지 않고, 무엇을 읽어 주는
  버튼인지가 자리로 드러난다.
- **소개 카드에서는 자동 재생하지 않는다.** 넘기기만 하는 카드라 소리를 밀어 넣지 않는다.
- **퀴즈 카드에서는 답한 직후 한 번 저절로 울린다.** 정답이든 오답이든 판정 직후가
  소리를 붙일 자리다 — 맞았으면 확인이고 틀렸으면 교정이다. 답하기 전에는 버튼 자체가
  없으므로 정답이 새지 않는다.
- 저절로 울린 것도 **누른 횟수 한 번으로 센다.** 듣고 나서 버튼을 누르면 0.8배가 나온다.
- 재생 실패로 버튼을 죽이지 않는다. 자동 재생이 막히거나 중단되는 것은 파일 문제가
  아니다. **파일이 없다는 판정은 오디오 `error` 이벤트 하나만** 한다.
- 소개 카드에서는 바로 누를 수 있다.
- 퀴즈 카드에서는 답하기 전에 **버튼이 아예 없다.** 정답이 읽기라서 답 전에 누르면
  정답이 그대로 들리는데, 그 잠금을 `disabled`가 아니라 **렌더 여부로** 건다.
  없는 버튼은 뚫리지 않는다.
- **누를 때마다 속도가 번갈아 바뀐다.** 홀수 번째는 `1.0`배, 짝수 번째는 `0.8`배다.
  한 번 듣고 못 알아들었을 때 다시 누르는 행동이 그대로 "천천히 다시"가 된다 —
  안내 문구도 두 번째 버튼도 필요 없다. 카드를 벗어나면 횟수는 0으로 돌아간다.
- 느린 재생은 **파일이 아니라 재생 시점**에 만든다(`playbackRate`). 음높이는 유지한다
  (`preservesPitch`) — 이게 없으면 목소리가 굵어져 발음 참조로 쓸 수 없다.
  파일을 느리게 구워 두면 그게 원본이 되어 정상 속도를 영영 못 낸다 (AUDIO.md).
- 발음 파일이 아직 없으면 비활성 상태로 둔다. 자리는 비우지 않는다 — 자리가 비면
  옆 글자가 밀린다.

---

## 4. 데이터 모델

**데이터베이스가 없다.** 콘텐츠는 레포 안의 JSON이고, 빌드 시점에 번들된다.

### 왜 DB를 안 쓰나 — 용량을 실측했다

| 항목 | 실측 (개념 3,000개 기준) |
|---|---|
| PNG 1024 생성 원본 (gitignore 대상) | 평균 883KB |
| **WebP 512 q80** (배포용) | 평균 **5.7KB** · 전량 16.6MB |
| MP3 (발음) | 평균 **12.7KB** · 전량 261MB |
| `content/*.json` | 6.5MB |
| **개념 1개당** | **약 95KB** (이미지 1장 + 발음 7개) |

처음 이 표를 쟀을 때는 개념당 12.5KB였다. 언어가 하나였기 때문이다 — **커진
것은 발음이다.** 이미지는 개념당 한 장을 일곱 언어가 나눠 쓰므로 언어를 더해도
늘지 않지만, 발음은 언어마다 한 개씩 붙는다.

| 개념 | 총량 |
|---:|---:|
| 1,000 | 93MB |
| **3,000 (지금)** | **278MB** |
| 5,000 | 464MB |
| 10,000 | 927MB |
**용량이 제약이 아니다.** 진도는 localStorage에 있고 쓰기 경로가 없으므로 서버가 할 일도 없다.

반대 방향의 마찰은 실재한다 — 무료 티어 DB는 무활동 시 일시정지되고, 개념을 하나씩 손으로
추가하는 워크플로(§7)에서는 어차피 매번 커밋하므로 "재배포 없이 콘텐츠 수정"이라는 장점도
살아나지 않는다.

DB는 **로그인과 진도 동기화가 필요해질 때** 도입한다 (§11).

### 타입

```ts
type Category = 'noun' | 'verb' | 'adjective' | 'scene'

type Concept = {
  slug: string            // 파일명이자 식별자. ^[a-z0-9-]+$
  meaning_ko: string      // 학습 언어와 무관한 한국어 뜻
  category: Category      // 오답 보기를 뽑는 근거. 생략 불가
  image_prompt: string    // 재생성용. STYLE_PROMPT는 포함하지 않는다
  words: Partial<Record<Language, Word>>
}

type Word = {
  term: string            // 표기.  ja: 猫 / de: Katze
  reading?: string        // 읽기.  ja: ねこ
  romanization?: string   // 로마자. ja: neko
  part_of_speech?: string
  example?: Example       // 한 줄일 때
  examples?: Example[]    // 여러 줄일 때. 문맥 카드가 회차로 돌려 쓴다 (§5)
  attributes?: Attributes
}
```

이미지와 오디오는 필드로 들고 다니지 않는다. **경로가 slug에서 결정되기 때문**이다.

```
public/concepts/{slug}.webp          이미지 — 언어 무관, 전 언어 공유
public/audio/{language}/{slug}.mp3   발음 — 언어별
```

**발음은 저장소에 그대로 둔다.** 개념당 7개(언어 수)라 지금 21,000개 · 261MB지만,
**Vercel에 배포하므로 옮기지 않는다** — Git 연동 배포는 빌드가 Vercel에서
돌아 `out/`이 그대로 서빙되고, 공개 문서에 정적 파일 개수 하드 캡이 없다. CLI로
직접 올릴 때 파일 수가 많아 걸리면 `vercel deploy --archive=tgz`를 쓰라는 안내가
있을 뿐이다([CLI 문서](https://vercel.com/docs/cli/deploy)). 배포당 파일 개수
한도(예: Cloudflare Pages 20,000개)가 있는 호스팅으로 옮길 때 다시 계산한다.

**옮길 길은 내 두었다.** 앱이 보는 주소는 `NEXT_PUBLIC_AUDIO_BASE`가 정한다 —
**비어 있으면 `public/`을 본다.** 채우면 그 앞자리가 붙어 CDN을 본다. 올리는 것은
`pnpm audio sync`(rclone, S3 호환이면 어디든)가 한다. 지금은 둘 다 쓰지 않는다.

옮길 신호는 셋이다. **감이 아니라 수치로 잰다** — 나중에 판단할 사람이
"무거워졌나"를 다시 고민하지 않게.

| 신호 | 기준 | 왜 |
|---|---|---|
| 저장소가 무거워짐 | **`.git` 500MB 초과** (지금 297MB) | GitHub 권장선은 1GB. 그 절반에서 움직여 여유를 남긴다. clone은 증분이 아니라 전부 받는다 |
| 발음 전량 재생성 | 목소리·포맷을 바꾸기로 **정한 시점** | 21,000개를 다시 뽑으면 히스토리에 사본이 한 벌 더 쌓여 `.git`이 두 배가 된다. **옮기고 나서 재생성한다 — 순서가 반대면 늦다** |
| 호스팅 이전 | 배포당 **파일 개수 한도가 있는 곳**으로 갈 때 | Cloudflare Pages는 20,000개. 지금 발음 21,000개에 이미지 3,000장이라 **이미 넘었다** — Vercel에 남는 이유가 이것이다 |

배치 30개당 약 2.8MB가 붙는다(mp3 210개 + 이미지 30장). 개념을 1,000개 더
넣어도 `.git`은 400MB대라 첫 신호까지는 여유가 있다.

**푸시는 증분이라 배치마다 2.8MB만 올라간다.** 무거워지는 것은 clone과 CI다 —
셋 중 첫 신호가 `.git` 크기인 이유가 그것이다.

파일이 있으면 쓰고, 없으면 이미지는 플레이스홀더로 폴백하고 발음 버튼은 비활성으로 둔다.
**플레이스홀더는 파일이 아니라 인라인 SVG다.** 바이너리를 하나 더 두고 관리할 이유가 없고,
토큰을 그대로 따라가며, 요청도 나가지 않는다.
`image_path` / `audio_path` 같은 컬럼이 없으므로 **파일과 데이터가 어긋날 여지도 없다.**

`decks`와 `deck_concepts`는 **없다.** 피드가 전체를 섞으므로 묶음이 필요 없다.

### 상식은 개념이 아니다 — 언어가 파일을 가른다

`Concept`은 일곱 트랙이 그림 한 장을 같이 쓴다. 상식은 그럴 수가 없다 — `は`가
わ로 읽히는 것은 일본어에만 있는 사실이라 다른 언어로 번역되지 않는다. 그래서
개념 옆에 **별개의 타입**을 세우고 파일을 언어로 가른다.

```ts
type Trivia = {
  id: string          // 파일 안에서 유일. ^[a-z0-9-]+$
  question: string    // 물음 한 문장
  choices: string[]   // 정답 1 + 오답 3
  answer: string      // choices 중 하나와 정확히 같다
  note: string        // 답한 뒤 뜨는 한 줄 — 왜 그런지
  source?: string     // 어느 노트에서 왔는가
}
```

`content/trivia/{lang}.json` 하나가 한 언어를 맡는다. 로더는 `lib/content.ts`가
같이 쥐고(정적 import), 순수 함수는 `lib/trivia.ts`에 있다 — 개념 쪽과 같은 이유로
갈라 둔다(번들러 없이 `node --test`가 돌아야 한다).

세 가지가 낱말과 다르다.

- **그림이 없다.** 물음 문장이 문제라 삽화를 지어 넣을 자리가 아니다 (§5).
- **오답이 콘텐츠다.** 낱말은 같은 category에서 자동으로 뽑지만(`distractorPool`)
  상식은 문항마다 그럴듯한 오답이 달라 기계가 못 정한다. 손으로 적고 `pnpm check`가
  넷인지·정답이 그 안에 있는지를 본다.
- **배열 순서가 커리큘럼이다.** 낱말은 개념 사이에 배우는 순서가 없어 아무거나
  먼저 나와도 되지만, 상식은 그렇지 않다 — 히라가나를 지나기 전에 접속법이 나오면
  답할 수가 없다. 그래서 새 카드를 **목록 앞에서부터** 낸다 (`lib/engine.ts`의
  `ordered`, 상식 덱에서만 켠다). 파일 안 순서는 brain의 학습 순서
  (`_data/notes_order.yml`)를 그대로 옮긴 것이고, 일곱 파일 모두 `~어_특징`으로
  시작한다.

  brain 폴더가 없어도 검사는 돌아야 하므로 `pnpm check`는 그 순서 자체를 확인하지
  않는다. 대신 **같은 노트의 문항이 앞뒤로 흩어졌는지**를 본다 — 문항을 파일 끝에
  그냥 붙이는 것이 순서가 깨지는 가장 흔한 경우이고, 그건 파일만 보고도 잡힌다.

### 참고 글은 푸는 것이 아니라 보는 것이다

`content/articles.json`이 참고 글을 담는다. 히라가나 표와 가타카나 표가 각각 한 편이다.
상식 탭에도 같은 지식이 문항으로 있지만(§4) 쓰임이 다르다 — **문항은 답해서 확인하는
것이고 글은 카드를 풀다 막혔을 때 열어 보는 것이다.** 그래서 채점도 진도도 없고,
엔진을 지나가지 않는다.

**목록이 먼저다.** 가나 표만 들어올 자리가 아니라서 글로 두고 목록을 세운다 — 탭으로
두면 셋째 글이 오는 순간 다시 짜야 한다. 글마다 `lang`을 들고 있어 그 언어 트랙에서만
목록에 서고, 글이 없는 트랙에서는 버튼이 자리째 빠진다.

**언어별로 파일을 가르지 않는다.** 지금 일본어 둘뿐이라 가르면 여섯 개가 빈 파일이
된다.

**표는 자리가 곧 뜻이다.** 오십음도에서 か행 세 번째 칸이 비면 く가 사라진 것이 아니라
표가 어긋난 것이다. 빈 칸은 `null`로 두고(や행의 yi·ye처럼 음운 체계에서 빠진 자리가
있다), `pnpm check`가 줄마다 칸 수와 열 수가 같은지 본다 — 칸이 모자라면 뒤 글자가
통째로 밀린다. 목록에서 제목만 보고는 열어 볼지 판단이 안 되므로 한 줄 설명도
비어 있으면 실패한다.

가타카나 표는 대응하는 히라가나를 같은 칸에 겹쳐 싣는다(`pair`). 두 문자를 나란히
보는 것이 가타카나를 익히는 가장 빠른 길이라 표를 둘로 나누지 않는다.

칸의 읽기(`roman`)는 **선택이다.** 가나는 헵번식 로마자를 두지만 병음처럼 글자
자체가 로마자인 문자에는 그 자리가 비고, 대신 성모가 없을 때의 표기(`i` → `yi`)나
음높이 설명이 온다.
- **진도 키에 앞자리가 붙는다.** `trivia:{lang}:{id}`. 낱말 slug와 절대 겹치지
  않아야 하고, 낱말 쪽은 이미 저장된 진도가 있어 slug 그대로여야 한다 (§6).

엔진은 이 차이를 모른다. 뽑기·예약·FSRS는 **키 하나만** 보므로(`lib/engine.ts`)
낱말과 상식이 같은 코드를 탄다.

### 파일은 주제고, 트랙은 언어로 고른다

`content/everyday.json` · `office.json` · `travel.json` · `food.json` · `home.json` ·
`body.json` · `nature.json` · `city.json`, 그리고 품사가 다른 `action.json`(동사)와
`quality.json`(형용사) · `scene.json`(상황 표현). 개념은 트랙에 속하지 않는다 —
`cat` 하나에 `words.ja` · `words.zh` · `words.es`가 나란히 들어가고, 각 트랙은
**자기 언어의 단어가 있는 개념만** 골라 출제한다.

파일을 트랙으로 나눴던 적이 있는데, 트랙이 둘일 때만 맞는 구조였다. 트랙이 여섯이
되면 같은 사물을 여섯 번 그려야 하고 §1의 전제("바나나 그림은 하나면 된다")가
무너진다. 파일은 이미지 작업을 몰아서 하기 좋은 단위 — 주제로 나눈다.

### 그림이 있는 개념만 출제한다

단어 목록은 **로드맵**이라 그림보다 앞서 쌓인다. 그림 없는 카드가 피드에 섞이면
학습이 아니라 빈칸 넘기기가 되므로, 파일이 있는 개념만 피드에 올린다.

판정은 **빌드 시점에 fs로** 한다(`app/page.tsx`). 정적 export라 그 결과가 결과물에
그대로 구워진다. 목록에만 있는 단어는 `/debug`에서 보이고, 그림을 넣는 순간 피드에
합류한다 — 등록 절차가 없다.

### `category`는 장식이 아니라 출제 규칙이다

덱이 사라졌으므로 오답 보기를 뽑을 근거가 `category`뿐이다 (§5).
명사 문제에 동사 보기가 섞이면 답이 그냥 보이므로 **생략을 허용하지 않는다.**
검증 스크립트가 누락을 실패시킨다 (§7).

### 한 개념에 한 언어의 단어는 하나다 — 의도된 제약이다

`words`가 언어를 키로 갖는 맵인 것이 이 제약이다. 그리고 이 제약이 개념 정의를 강제한다.

일본어 `水`(찬물)와 `お湯`(더운물)은 영어로는 둘 다 water지만, **개념을 쪼갠다**:
`cold-water`, `hot-water`. 개념은 사전 표제어가 아니라 **시각화 가능한 지시체** 단위다.
이미지가 하나로 그려지지 않으면 개념이 둘이라는 뜻이다.

### 이미지가 없는 개념은 없다

추상어처럼 그릴 수 없는 개념은 **콘텐츠에 넣지 않는다.** 카드 5종이 전부 이미지를 전제하기 때문이다.

`public/concepts/{slug}.webp`가 없는 것은 "이 개념은 그림이 없다"가 아니라
**"아직 생성 전"** 이라는 뜻이다. 그 경우 앱은 **공통 플레이스홀더 한 장**으로 대체하고 정상 출제한다.

### 언어별 학습 전략은 설정표로

어느 필드가 정답인지는 언어마다 다르다. 코드에 박지 않는다.

```ts
type LangStrategy = {
  answer: 'reading' | 'term'   // 보기·정답·빈칸에 쓰는 필드
  aside: ('term' | 'romanization')[]  // 소개 카드 참고줄
}

const LANG: Record<string, LangStrategy> = {
  en: { answer: 'term',    aside: [] },
  ja: { answer: 'reading', aside: ['romanization', 'term'] },
  // de: { answer: 'term',    aside: [] },
  // zh: { answer: 'reading', aside: ['term'] },
}
```

`answer` 필드가 비어 있는 단어는 그 언어에서 출제되지 않는다.

### 언어별 속성은 `attributes`로 몰아넣는다

필드를 언어마다 늘리지 않는다. TypeScript에서 언어별 타입만 좁혀 안전성을 확보한다.

```ts
type Attributes =
  | { jlpt?: 'N5'|'N4'|'N3'|'N2'|'N1'; pitchAccent?: number }  // ja
  | { article?: 'der'|'die'|'das'; plural?: string }           // de
  | { tones?: number[] }                                       // zh
```

---

## 5. 카드 5종

같은 단어가 숙련도에 따라 다섯 가지 모습으로 나온다.
난이도 순서는 기억 연구의 표준 분류를 따른다 —
**Recognition(재인) ≈ Aided Recall(단서 회상) < Recall(순수 회상)**. 재인이 가장 쉽다.

순수 회상 칸은 **두지 않는다.** 정답을 보고 스스로 맞았다고 판정하는 방식은
채점이 주관적이라 진도 데이터를 오염시킨다. **객관적으로 채점되는 카드만 남긴다.**

| rung | 이름 | 테스트 | 화면 |
|---|---|---|---|
| 0 | 소개 | 없음 | 이미지 + 읽기 + 뜻 + (한자·로마자 참고) |
| 1 | 재인 | Recognition | 이미지 + 읽기 보기 4개 (2×2) |
| 1' | 듣기 | Recognition | **소리만** + 그림 보기 4개 (2×2) |
| 2 | 문맥 | Cued Recall | **그림 없이** 예문의 낱말 자리를 빈칸 + 낱말 보기 4개 |
| 3 | 단서 회상 | Aided Recall | 이미지 + 읽기 일부를 빈칸 + 후보 글자 4개 |
| — | 상식 | Recognition | **이미지 없이** 물음 한 줄 + 보기 4개 (상식 덱 전용) |

듣기는 rung을 따로 갖지 않는다. **더 어려운 단계가 아니라 같은 재인을 다른
감각으로 하는 것**이라 재인 칸에서 회차에 따라 번갈아 나온다 — 짝수 회차는
그림 보고 낱말 고르기, 홀수 회차는 소리 듣고 그림 고르기다.

재인과 철자 사이가 비어 있었다. 그림을 보고 이름을 고르는 것과 철자 한 글자를
떠올리는 것 사이에는 **문장 안에서 그 낱말이 설 자리를 아는가**라는 단계가 있다.

상식 카드는 사다리에 서지 않는다. 소개할 그림도, 뚫을 철자도, 넣을 예문도 없어
**4지선다 한 칸이 전부**다. rung을 없애는 대신 위아래 끝을 같은 칸으로 좁혀
(`TRIVIA_LADDER`) 진도·예약·숙련도 규칙이 그대로 돌게 한다 (§6).

**문항 지시문을 쓰지 않는다.** 이미지와 보기만으로 무엇을 묻는지 자명하다.
상식 카드만 예외로 물음 문장이 있다 — 그림이 없어 무엇을 묻는지 말해 줄 것이
문장뿐이다.

### 0. 소개

전부 노출한다. 읽고 넘긴다. 마찰 0 — 숏폼의 진입 감각을 그대로 둔다.

본문은 **좌측 정렬**이며 위에서부터 세 덩어리다.

```
ねこ                    🔊     읽기 — 가장 크다. 오른쪽 끝에 발음 버튼
[neko] · 猫                   발음 보조 · 표기. --sub 작은 줄
명사  고양이                   품사 알약 + 뜻
또는 гостиница                같은 뜻의 다른 말. 있을 때만
```

- **첫 참고 항목이 발음 보조 자리다.** 큰 글자가 이미 읽기이므로 그 자리에 오는 것은
  로마자이고, 대괄호로 감싼다. 순서는 `LANG[lang].aside`가 정한다.
- 표기와 읽기가 같은 단어(`バナナ`)는 참고줄에서 표기를 빼 중복을 없앤다.
- **같은 뜻의 다른 말은 뜻줄 아래에 조용히 붙인다** (`Word.also`). 호텔은 `отель`이자
  `гостиница`이고 신발은 `туфли`이자 `обувь`인데, 그림이 같으므로 개념을 나눌 수 없고
  하나만 적으면 나머지를 못 배운다. **정답으로도 오답으로도 쓰지 않는다** — 정답이
  둘이면 4지선다에 답이 두 개 깔리고 빈칸은 어느 철자를 받을지 정할 수 없다.
  로마자와 같은 취급이다. 뜻이 **다르면** 여기 넣지 않고 개념을 나눈다 (§2 표).
  `pnpm check`가 표제어와 같은 표기를 막는다. 다른 개념의 정답과 겹치는 것은 막지
  않고 **참고로만 적는다** — 걸리는 것이 하나같이 진짜 다의어이기 때문이다.
  `apartment`의 `flat`, `autumn`의 `fall`, `necktie`의 `tie`는 영어에서 두 뜻을 다
  갖는다. 오답으로 만나지도 않는다 — 오답은 같은 주제·같은 품사에서 오는데
  `necktie`는 clothes의 명사, `tie`는 action의 동사다
  이때 참고줄은 `[banana]` 하나만 남는다.
- **예문 소리는 파일이 있을 때만 버튼이 뜬다.** 낱말 발음 버튼과 달리 비활성으로
  남기지 않는다 — 낱말은 언젠가 다 채울 자리라 빈 버튼이 자리를 지켜야 글자가 안
  밀리지만, 예문은 아직 한 자리도 없어서 3,031장에 회색 버튼만 늘어놓게 된다.
- 파일 이름은 **문장의 지문**이다 — `public/audio/{lang}/ex/{slug}-{i}-{해시12}.mp3`.
  낱말은 `{slug}.mp3`로 충분하지만 예문은 자주 바뀐다(한 세션에 870개를 갈아 끼운
  적이 있다). `{slug}-{i}`로 지었다면 파일이 그대로 남아 **문장과 다른 소리**가 났을
  것이다 — 파일이 있으니 버튼은 켜지고 학습자는 틀린 소리를 듣는다. 해시를 넣으면
  예문을 고쳤을 때 이름이 달라져 버튼이 조용히 사라지고, 남은 파일은 `pnpm check`가
  잡는다. `slug-i`를 앞에 두는 것은 사람이 찾을 수 있어야 해서다.
- 해시는 FNV-1a 64비트의 아래 48비트다. 브라우저와 스크립트가 같은 값을 내야 해서
  의존성 없이 짠다 — WebCrypto의 sha256은 비동기라 렌더 중에 못 쓴다.
- 있는 자리를 적는 목록도 낱말과 **방향이 반대**다. 낱말은 거의 다 차 있어 없는 것을,
  예문은 거의 다 비어 있어 있는 것을 적는다 (`EXAMPLE_AUDIO`).
- 품사는 `part_of_speech`를 그대로 알약에 넣는다. 값을 코드로 매핑하지 않으므로
  언어가 늘어도 그대로 굴러간다. 필드가 없으면 알약만 빼고 뜻을 그린다.
  `category`는 출제 규칙 전용이라(§4) 화면에 드러내지 않는다 — `scene`은 품사가 아니다.
- 레벨이 있으면 **시트 좌하단에 한 줄**로 적는다(`N5` · `HSK 1` · `A1`). 시험마다
  등급 체계가 달라 하나로 합치지 않고 `attributes`에 있는 것을 그대로 읽는다
  (`jlpt` · `hsk` · `cefr` · `tsl`). 배지가 아니라 `--sub` 색의
  조용한 텍스트다 — 학습 대상이 아니라 참고 정보라서 시선이 마지막에 닿는 자리에 둔다.
  값이 없으면 자리째 빠진다. **없는 등급을 지어내지 않는다.** TOEIC은 등급이
  아예 없어서 `TSL 19`처럼 목록 이름과 순위를 그대로 적는다 — 등급인 척하지 않는다.
- **정답 길이에 맞춰 글자를 줄인다** (`lib/fit.ts`). 카드는 낱말 하나를 전제로
  만들어졌는데 `scene`의 정답은 문장이다. 품사가 아니라 **글자 수**로 정하는 이유는
  짧은 표현(`すみません`)과 긴 명사(`ショッピングモール`)가 섞여 있어서다 — 카테고리로
  나누면 둘 다 틀린 크기가 된다. 보기도 길면 2열에서 1열로 바꾼다.
- **4지선다에는 「모르겠어요」가 있다.** 찍어서 맞히면 FSRS에는 "안다"로 기록되고
  그 한 번이 다음 복습을 몇 주 뒤로 민다. 모를 때 모른다고 말할 자리를 준다 —
  누르면 오답과 같게 처리하고(`Rating.Again`) 정답을 바로 드러낸다.
  **보기와 같은 모양이면 안 된다.** 테두리 있는 사각 버튼으로 두면 다섯 번째
  보기로 읽힌다. 밑줄만 있는 조용한 글자 버튼으로 두고, 답한 뒤에는 자리째 없앤다.
- **상황 표현은 빈칸 칸으로 올라가지 않는다.** 문장에서 한 글자를 뚫어 봐야 나머지가
  다 보여 회상 훈련이 되지 않고, 글자별로 늘어놓는 빈칸 카드가 화면을 넘긴다.
  `canBlank`가 막는다 — 그 단어는 재인 칸(4지선다)에 머문다.
- 하단에 **예문 한 줄**을 둔다. 구분선 아래 かな 문장과 한국어 뜻 두 줄이다.
  - **일본어·중국어 예문에는 로마자 줄을 하나 더 둔다.** 낱말에는 로마자가 있는데
    예문에는 없으면 그 줄만 소리 내 볼 수 없다. 손으로 쓰지 않고 `pnpm romanize`가
    규칙으로 채운다 — 일본어는 かな 표를, 중국어는 CC-CEDICT 낱말 단위 최장 일치를
    쓴다 (§7). 유럽 언어는 표기가 곧 읽기라 이 줄이 없다.
  - **동사는 예문을 원형으로 쓴다.** 정답이 사전형(`たべる`·`comprar`)인데 예문에서
    활용해 버리면 정답이 화면에 안 보인다. `パンを たべる。` · `Quiero comprar pan.`
    처럼 원형이 그대로 오는 자리를 쓴다 — `pnpm check`가 이 규칙을 강제한다.
  - **かな로 쓰고 띄어쓰기로 끊는다.** 학습 대상이 읽기이므로 한자를 쓰지 않고,
    かな만 이어 붙이면 읽기 어려우므로 어절을 띄운다 — `パンを かいます。`
  - 예문에는 그 단어가 반드시 들어간다. `pnpm check`가 빠졌으면 경고한다.
  - **퀴즈 카드에는 예문을 넣지 않는다.** 정답이 문장에 그대로 들어 있다.
  - 예문에는 발음 버튼을 붙이지 않는다. 발음은 단어 하나만 들려준다.
  - 필드가 없으면 그 줄을 통째로 그리지 않는다. 구분선도 함께 사라진다.

### 1. 재인 — 4지선다

- 정답은 `LANG[lang].answer` 필드값
- **오답 3개는 회차에 따라 조여진다.** 처음 두 번은 같은 `category` 전체에서,
  세 번째부터는 **같은 주제 파일**에서 뽑는다 (`NEAR_FROM_ATTEMPT`).
  - 재인 칸은 그 낱말을 처음 시험하는 자리다. 여기서 오답을 촘촘히 깔면 처음 본
    낱말을 바로 정밀 변별로 묻게 되어 초반 승률이 떨어진다 (§6)
  - 그렇다고 계속 넓게 두면 `towel` 문제에 `photographer`가 깔린다 — "욕실 물건
    같은 것"만 떠올려도 셋이 지워져, 낱말을 아는지가 아니라 어느 분야인지를 묻는다
  - 그래서 **같은 칸이 회차마다 조여진다.** 사다리를 늘리지 않고 난이도만 올린다
  - 같은 주제에 셋이 모자라면 넓은 풀로 돌아간다
- 2×2 그리드. 보기 하나가 최소 44px 이상
- 정답 → 초록 / 오답 → 고른 것 빨강 + 정답 초록

### 1'. 듣기 — 소리 듣고 그림 고르기

재인 칸과 **문맥 칸 두 곳**에 번갈아 끼어든다. 재인에서는 같은 재인을 다른
감각으로 하려는 것이고, 문맥에서는 이유가 하나 더 있다 — **같은 예문이 반복되는
것을 절반으로 줄인다.**

**홀짝으로 가르지 않는다.** 답할 때마다 rung과 회차가 같이 1씩 움직여
`rung + attempt`의 홀짝이 변하지 않기 때문이다 — 한 칸에 머무는 동안 늘 같은
쪽만 나온다. 그래서 회차를 섞은 해시로 던진다(`isListenTurn`). 같은 회차면 같은
결과라 카드를 다시 그려도 종류가 바뀌지 않는다.

- 위쪽 그림 자리에는 **소리 버튼만** 둔다. 정답 그림을 띄우면 듣지 않고 풀린다
- 답한 뒤 그 자리에는 그림이 아니라 **낱말·읽기·뜻·예문**이 들어온다. 정답 그림을
  다시 넣으면 보기 넷 가운데 하나가 이미 초록 테두리로 서 있어 같은 그림이 한 화면에
  두 번 보이고, 두 번째 그림은 새로 알려주는 것이 없다. 듣기 카드는 소리에서 뜻으로
  곧장 가느라 **철자를 한 번도 보지 않고** 지나가므로, 답한 뒤가 그것을 볼 유일한
  자리다. 아래 `Reveal` 줄은 뺀다 — 뜻과 소리 버튼을 이 판이 이미 들고 있다
- 보기는 낱말이 아니라 **그림 4개**다. 글자를 깔면 듣기가 아니라 받아쓰기가
  된다 — 소리를 철자로 옮긴 다음에야 고를 수 있기 때문이다
- 마운트 직후 한 번 저절로 울리고, 다시 누르면 0.8배로 느려진다 (§3의 발음 버튼)
- **저절로 울리지 못하면 버튼이 기다리는 모양이 된다.** iOS 사파리는 사용자가
  건드리지 않은 소리를 막는다 — 넘기는 손짓은 제스처지만 재생은 그 손짓의 호출
  스택 밖에서 일어나므로 막힌다. 지시문을 쓰지 않으므로(위) "눌러서 들으세요"를
  띄우는 대신 강조 테두리와 맥박으로 눌러야 한다는 것을 말한다. 한 번 울리고
  나면 그 표시가 사라진다
- **발음이 없으면 이 카드를 만들지 않는다.** 소리가 유일한 단서라 파일이 없으면
  문제가 성립하지 않는다. 정적 내보내기라 도는 중에 파일 존재를 물어볼 서버가
  없으므로 **없는 자리만** `lib/audio-have.ts`에 적어 두고 그걸 본다.
  `node scripts/audio.ts manifest`가 만들고, 낡으면 `pnpm check`가 경고한다 —
  콘텐츠를 넣고 발음을 아직 안 만들었는데 목록이 옛날 그대로면 빈 소리가 난다

### 2. 문맥 — 예문 빈칸

- 예문에서 정답 낱말을 통째로 뚫고 **낱말 보기 4개** 중 고른다
- **그림을 답하기 전에는 보여주지 않는다.** 수건 그림을 띄워 놓고
  `The ___ is clean.`을 물으면 문장을 읽지 않고도 답이 되어 재인 칸과 같은
  문제가 된다. 자리는 비워 두되 없애지 않는다 — 없애면 카드 높이가 달라져
  넘길 때 튄다. 답한 뒤에 그림이 나오고 빈칸이 정답으로 채워진다
- **그 문장에 들어가도 말이 되는 낱말은 오답에서 뺀다**(`clozePool`). `___を きる。`
  같은 흔한 틀에는 그 주제의 옷이 전부 들어맞아 정답이 둘 이상인 문항이 됐다. 어떤
  낱말이 이 자리에 들어가도 되는지는 그 낱말의 **자기 예문**이 알려준다 — 정답을 뚫고
  남은 문장이 같으면 넣어도 맞는 낱말이므로 뺀다. 셋이 안 남으면 원래 풀로 돌아간다.
  증명되는 것만 거른다 — 틀이 달라도 뜻으로 들어맞는 오답은 예문을 고쳐야 한다
- 오답 3개는 **같은 주제 파일**에서 먼저 뽑는다(`nearPool`). category만 보면
  `The ___ is clean.`에 `downstairs`가 섞이는데, 문법으로는 들어가지만 문맥으로는
  어울리지 않아 문장을 읽지 않고도 걸러진다. 같은 주제에 셋이 모자라면 넓은
  풀로 돌아간다 — 문항이 안 만들어지는 것보다 오답이 헐거운 편이 낫다
- 주제는 JSON에 없다. 로더가 파일 이름을 붙인다 (`lib/content.ts`) — 개념을
  다른 파일로 옮기면 값이 따라 바뀌어야 하는데 JSON에 적어 두면 두 곳을 맞춰야 한다
- **상황 표현도 이 칸을 만든다.** 오래 막아 두었다 — 표현 자체가 문장인데 예문을
  또 두는 것이 겹쳐 보였기 때문이다. 그래서 상황 표현은 소개와 재인 두 칸만 돌았다.
  뚫을 것이 표현 **전체**라는 점이 오히려 이 칸을 성립시킨다. 표현을 감싸는 한 줄이
  상황을 만들고(`이 자리 있나요?`) 빈칸에는 표현이 통째로 들어간다 — 낱말 카드가
  "이 자리에 어떤 낱말이 오는가"를 묻듯, 여기서는 **어떤 상황에 어떤 말을 하는가**를
  묻는다. 오답은 같은 파일에서 오므로 후보 넷이 모두 상황 표현이고, 상황을 읽지
  않으면 고를 수 없다
- 표현이 물음표로 끝나면(`몇 시예요?`) 문장 **끝**에 놓는다. 앞에 상황을 두고
  뒤에 표현을 붙이는 형태다 — 표현이 그대로 보여야 뚫리는데, 물음표까지 표제어라
  중간에 넣으면 문장이 두 번 끝난다
- 정답이 예문 안에 **온전한 낱말로** 있어야 뚫린다. 들어 있기만 해서는 안 된다 —
  `Wash your hands.`에서 `hand`를 뚫으면 `Wash your ___s.`가 되어 꼬리가 정답의
  모양을 알려주고, `В чайнике остыл чай.`에서는 앞의 `чайнике`가 뚫려 **진짜 정답이
  문장에 그대로 남는다.** 그래서 앞뒤가 글자가 아닌 자리만 고른다(`clozeAt`).
  띄어쓰기가 없는 일본어·중국어에는 이 규칙을 적용하지 않는다 (`LangStrategy.spaced`).
  `pnpm check`가 모든 언어에서 이것을 강제한다(경고 0건)
- **예문이 여럿이면 회차로 돌린다** (`examples`). 하나뿐이면 같은 문장이 다시
  나오는데, 그 반복은 듣기 카드가 번갈아 끼어들어 절반으로 줄인다
- 소개 카드는 **첫 줄만** 보여준다. 소개의 일은 한 번 보여주는 것이지 다
  보여주는 것이 아니다

### 3. 단서 회상 — 빈칸

- 읽기의 한 글자를 빈칸으로 만든다
- **OS 키보드를 쓰지 않는다.** 후보 글자 4개를 탭해서 채운다 (IME 의존 제거)
- 오답 후보는 **같은 문자 체계에서 닮은 글자**를 쓴다 — 표기 정확도까지 잡힌다.
  - 일본어 — 모양이 비슷한 가나 (`ナ / タ / ソ / ン`)
  - 알파벳 — 모양이 닮았거나 **소리가 겹치는** 것 (`c / k / s`, `a / e / o`).
    발음 부호는 벗기고 찾는다 — `ó`의 닮은 글자는 `o`의 것과 같다
  - 한자 — 부수나 획이 겹치는 것 (`蕉 / 蔬 / 薯 / 藻`, `包 / 句 / 勺 / 旬`).
    획수가 전혀 다른 글자를 깔면 소거법으로 풀린다
  - 키릴 — 세로줄이 늘어선 글자(`и / й / н / п`)와 부호 글자(`ы / ь / ъ`),
    그리고 강세 없이 뭉개지는 모음(`а / о`). 러시아어 철자 오류가 실제로
    나는 두 자리다 — `корова`를 `карова`로 적는 쪽과 `и·н·п`를 헷갈리는 쪽
- **아포스트로피는 곧은 것(`'`) 하나로 쓴다.** 굽은 `’`와 섞이면 표제어와 예문이
  서로 다른 글자를 쓰게 되어 문맥 카드가 낱말을 못 찾는다. 프랑스어에만 나오고
  `pnpm check`가 막는다 — 표기가 예뻐지는 것보다 두 곳이 어긋나지 않는 것이 중요하다
- **잇는 부호는 뚫지 않는다.** 공백·붙임표·따옴표가 그렇다 — `código de barras`,
  `x-ray`, `U-Bahn`, `секонд-хенд`, `aujourd'hui`. 뚫을 수 없는 것은 그 부호
  자리이지 낱말 전체가 아니다. 이 부호가 든 낱말 137개가 이 칸에 오른다
- 닮은 후보를 댈 수 없는 글자는 그 자리를 건너뛴다. 뚫을 자리가 하나도 없으면
  그 단어는 재인 칸에 머문다.
- 읽기가 한 글자인 단어(`き`, `め`)는 빈칸이 성립하지 않으므로 rung 1에 머문다
- **세 번에 한 번은 문맥 카드가 대신 나온다** (`isClozeTurn`). 꼭대기는 위로 갈 데가
  없어서 한 낱말이 여기 앉으면 남은 복습이 전부 한 글자 빈칸이었다 — 뚫는 자리는
  돌아가지만 카드 모양은 끝까지 하나다. 아래 두 칸이 듣기와 번갈아 도는 것과 같은
  이유이고, 듣기를 넣지 않는 것은 듣기가 재인이라 두 칸 아래이기 때문이다.
  절반이 아니라 3분의 1인 것도 같은 이유다 — 꼭대기에서 훈련할 것은 회상이고
  변화는 지루함을 깨는 만큼만 있으면 된다

---

## 6. 학습 엔진

모든 상태는 클라이언트에 있다. **진도는 트랙별로 분리된다** — `lingo.progress.{track}`.

### 왜 Leitner를 버렸나

Leitner는 "오늘 복습할 것"을 날짜로 정한다. 무한 피드에는 오늘도 마감도 없다.
알고리즘 비교에서 Leitner가 지적받는 지점도 정확히 우리 문제다 —
**같은 상자 카드를 전부 동일하게 취급하고, 간격이 사용자에게 적응하지 않는다.**

대신 **두 층으로 나눈다.** 각 층이 답하는 질문이 다르다.

| 층 | 답하는 질문 | 단위 | 도구 |
|---|---|---|---|
| **시간 층** | 이 카드를 오늘 꺼낼까, 2주 뒤에 꺼낼까 | 일(day) | `ts-fsrs` |
| **피드 층** | 지금 이 자리에서 몇 장 뒤에 다시 꽂을까 | 장(card) | 직접 구현 |

FSRS의 망각곡선은 일 단위다. 방금 맞힌 카드를 3분 뒤에 물으면 회상확률이 거의 안 떨어져
가중치가 0에 가깝다. **한 번 앉아서 스와이프하는 동안의 재등장은 FSRS가 못 정한다.**
그 구간을 피드 층이 맡는다.

### 시간 층 — ts-fsrs

`ts-fsrs` (FSRS v6, MIT, 의존성 0). SM-2 대비 같은 retention에 리뷰 20~30% 감소가 보고돼 있다.

```ts
import { createEmptyCard, fsrs, Rating } from 'ts-fsrs'

const scheduler = fsrs()
const next = scheduler.next(card, new Date(), correct ? Rating.Good : Rating.Again)
const R = scheduler.get_retrievability(next.card, new Date(), false)  // 0~1
```

등급은 4개지만 채점이 2진이므로 **`Good`(정답) / `Again`(오답)만** 쓴다.

### 피드 층 — 다음 카드 뽑기

```
1. 예약 큐에 도래한 카드가 있으면 그것을 낸다
2. 없으면 15% 확률로 미소개 단어 (신규 유입)
3. 나머지 85%는 (1 - R) 가중 무작위 추출, 최근 K장 쿨다운 제외
```

**예약 거리** — 답한 카드를 이 거리만큼 뒤에 다시 꽂는다.

| 상황 | 거리 |
|---|---|
| 소개 직후 | 3장 |
| 정답 | 8장 |
| 연속 2회 정답 | 20장 |
| 오답 | 2장 |
| 꼭대기에서 연속 4회 정답 | 예약하지 않는다 |

**꼭대기에서 내리 맞히는 카드는 놓아준다.** 예약 큐는 뽑기의 1순위라 가중치를 통째로
건너뛴다. 그래서 맞힐 때마다 다시 예약하면 rung 3에 앉은 낱말이 20장마다 **영원히**
돌아온다 — 다 외운 단어의 한 글자 빈칸이 계속 나오는 증상이 이것이었다. 예약은 아직
배우는 중인 카드를 한 세션 안에 다시 만나게 하려는 장치이고, 그 위의 간격은 날짜
단위라 FSRS가 정한다. 놓아주면 회상확률이 1에 가까워 가중치가 바닥(0.02)에 붙고,
하루가 지나 확률이 떨어지면 저절로 다시 올라온다. 한 번 틀리면 예약이 다시 붙는다.

**신규 유입 15%는 조건부가 아니라 고정 비율이다.**
"복습이 다 끝나면 새 단어"처럼 미루면 피드가 굳는다. 추천 시스템이 노벨티를 확률적으로
강제 주입하는 이유와 같다 — 신선도가 없는 피드는 content fatigue로 붕괴한다.
프로덕션 관행 수치대(신선 콘텐츠 약 10%)와도 맞다.

**신선도 부스트** — 막 소개한 단어는 한동안 가중치에 가산점을 주고 지수적으로 깎는다.

> 확장 간격(3 → 8 → 20)을 정밀하게 튜닝할 필요는 없다.
> Landauer & Bjork의 확장 스케줄 우위는 단기(10분 후)에만 재현되고,
> 2일 후 테스트에선 균등 간격이 더 좋았으며, 4주 어휘 학습에선 8주 후 결과가 동등했다.
> **간격이 벌어진다는 사실 자체가 중요하지, 수열의 정확한 모양이 중요한 게 아니다.**

### 저장 형식

```ts
// localStorage key: `lingo.progress.{language}`   예: lingo.progress.ja
type Progress = {
  version: 2
  cards: Record<string, {        // key = word.id (언어마다 따로 외운다)
    rung: 0 | 1 | 2 | 3          // 카드 타입 (§5)
    streak: number               // 연속 정답 수. 예약 거리 계산용
    fsrs: SerializedFSRSCard     // due·last_review는 timestamp로
  }>
  introducedAt: Record<string, number>  // wordId → 최초 노출 시각. 신선도 부스트용
}

// localStorage key: `lingo.language`  → 'ja'
```

`cards`에 항목이 없으면 미소개 단어다.

### rung 이동

- **소개 카드를 넘김** → `rung = 1`, FSRS 카드 생성
- **정답** → `rung = min(rung + 1, 3)`, `streak += 1`, `Rating.Good`
- **오답** → `rung = max(rung - 1, 0)`, `streak = 0`, `Rating.Again`

오답은 **한 칸만 내린다.** 바닥까지 떨어뜨리지 않는다.
승률이 무너지면 무한 스와이프의 동력이 끊긴다.

**버전이 오르면 진도를 버리지 않고 옮긴다.** 사다리 가운데에 문맥 칸이 끼면서
철자 칸이 2에서 3으로 밀렸다 — v1에서 2였던 카드는 3으로 올린다. 그대로 두면
철자까지 올라간 카드가 문맥 칸으로 강등된다. 진도는 서버에 사본이 없어서 한 번
버리면 몇 달치 복습 간격이 사라진다.

### 완전히 외웠다는 것

헤더 숙련도(§3)가 세는 기준이다.

```
rung === 2  &&  fsrs.stability >= 21   // 단위: 일
```

**rung만으로는 못 센다.** rung 2는 소개를 지나 정답 두 번이면 닿으므로 한 세션
스와이프로 채워진다. 그렇게 센 숫자는 외운 개수가 아니라 스와이프 횟수다.

FSRS의 stability는 다르다. **같은 날 다시 맞혀도 자라지 않고**(경과 0일)
날짜를 넘겨 살아남아야 오른다 — 실측으로 2.3 → 11 → 46 → 163일이다.
21일을 넘기려면 최소 서너 날에 걸친 복습이 필요하다.
**문턱이 시간을 요구한다는 점 자체가 이 값을 고른 이유다.**

되돌아가는 것도 허용한다. stability 46일까지 올라간 카드가 한 번 틀리면
3일 아래로 무너져 곧바로 빠진다. 저장할 필드가 따로 없다 — 판정은 언제나
지금의 `rung`과 `stability`에서 나온다.

localStorage가 없거나 깨졌으면 빈 진도로 초기화한다. 마이그레이션은 `version` 필드로 처리한다.

---

## 7. 콘텐츠 파이프라인

단일 진실 소스는 **레포 내 JSON 파일**이다. 덱이 없으므로 파일 분할은 편의일 뿐이고,
앱은 전부를 하나의 풀로 읽는다.

```
content/nouns.json
content/verbs.json
```

```jsonc
{
  "concepts": [
    {
      "slug": "cat",
      "meaning_ko": "고양이",
      "category": "noun",
      "image_prompt": "one sitting cat facing forward, tail curled around its front paws",
      "words": {
        "ja": {
          "term": "猫",
          "reading": "ねこ",
          "romanization": "neko",
          "part_of_speech": "명사",
          "attributes": { "jlpt": "N5" }
        }
      }
    }
  ]
}
```

**언어 추가는 각 개념의 `words`에 블록 하나를 더 쓰는 일이다.**
이미지도, 뜻도 건드리지 않는다.

`image_prompt`는 **내용만** 쓴다. 스타일 문구는 절대 여기 쓰지 않는다 —
`IMAGE_STYLE.md`의 `STYLE_PROMPT`가 앞에 붙는다.

### 얼마나 넣어야 하나 — `pnpm coverage`

**개념 수로는 답이 안 나온다.** 3,000개를 넣고도 HSK 1~3급의 5분의 1밖에
못 덮는다 — 목록을 따라가지 않고 **그릴 수 있고 일곱 언어가 공유하는 것**을
고르기 때문이다. HSK 1급에도 `因为`·`可以` 같은 기능어가 많은데 이미지
규칙상 못 들어간다. 그러니 진도는 목록에 대고 재고, 그 일은 스크립트가 한다.

```bash
pnpm coverage                  # 숫자만
pnpm coverage --missing tsl    # 빠진 낱말을 실제로 찍는다 (tsl · hsk · torfl)
```

**숫자만으로는 다음에 뭘 채울지 못 정한다.** "656개 남음"은 일감이 아니라
성적표다. `--missing`은 그 656개를 그대로 찍어 배치 후보로 만든다 — TSL은
빈도 순이라 위에서부터 고르면 된다. 목록에 기능어가 섞여 있는 것은 여기서도
그대로 보인다(HSK 1급의 `的`·`吧`). 그릴 수 없는 것은 건너뛴다.

**빠진 것이 다 같은 빠짐은 아니다.** `--missing`은 셋으로 가른다.

| 갈래 | 뜻 |
|---|---|
| 표제어 안에 든 것 | 상황 표현·합성어 안에 있다 — `остановка`는 `какая следующая остановка` 안에 |
| 예문에만 있는 것 | 카드로는 안 묻지만 읽기는 한다 |
| **정말 없는 것** | 우리 글 어디에도 없다. **이것만 배치 후보다** |

TORFL A1은 423개가 빠졌지만 정말 없는 것은 253개고, HSK 1급은 364개 중
83개다 — 나머지는 이미 어딘가에서 만난다. 중국어는 띄어쓰기가 없어 경계를
못 보므로 그냥 들어 있는지로 센다.

**숫자를 이 문서에 박지 않는다.** 배치를 한 번 돌 때마다 낡는다. 대신 읽는
법을 적어 둔다.

| 줄 | 뜻 | 어떻게 볼까 |
|---|---|---|
| 주제 축 | 주제 파일의 개념 수를 적은 순으로 | 편차가 벌어지면 어떤 주제는 사흘이면 바닥난다. **얇은 축부터 채운다** |
| 품사 파일 | `action`·`quality`·`scene`을 따로 | 주제가 아니라 품사라 **분모가 다르다.** 주제 표와 같이 놓고 비교하지 않는다 |
| — | — | 축이 얇다고 다 채울 수 있는 것은 아니다. `time`이 그렇다 — 달 열둘과 요일 일곱, `자주`·`가끔` 같은 때 부사가 그 자리를 메울 자연스러운 낱말인데 **1월과 7월을 그림으로 가를 수 없어** 못 넣는다. 얇은 이유가 게을러서가 아니라 주제의 성질이면 그대로 둔다 |
| TSL | TOEIC 표제어 1,250개 중 우리가 덮은 것 | **끝이 있는 유일한 목록.** 남은 수가 그대로 할 일이다 |
| HSK 급별 | HSK 3.0 목록 대비 | 기능어가 많아 100%는 도달할 수 없다. 추세만 본다 |
| TORFL 등급별 | ТРКИ 최소치 A1~B2 대비 | 등급은 **누적이 아니다** — 낱말이 처음 나오는 등급에만 센다 |
| 등급이 붙은 낱말 | 우리 콘텐츠 중 등급이 채워진 비율 | **분모가 다르다.** 목록 대비가 아니라 출처가 얼마나 물어 주는지를 본다 |
| 콘텐츠 구성 | 품사·상황표현 비율 | 명사가 70%를 넘으면 낱말 사전이 된다. scene은 10% 언저리를 지킨다 |

목록 완주는 목표가 아니다. 어휘 연구의 통설로 일상 텍스트의 80%가 2,000
단어군, 구어의 95%가 3,000 단어군이라 **언어당 2,000개를 넘긴 시점부터는
"부족한 자리를 채우는 일"**이지 "목록을 끝내는 일"이 아니다. 실질 상한은
어휘량이 아니라 §9 #5의 피드 소진이다.

---

### 개념은 하나씩 추가한다

**목록은 미리 쌓아도 되고, 그림은 하나씩 그린다.**

한동안은 목록조차 미리 만들지 않았다. 그림 없는 개념이 쌓이는 것이 문제였기 때문이다.
그림이 있는 개념만 출제하기로 하면서(§4) 그 근거가 사라졌다 — 쌓여도 피드에 나오지
않으니 무해하고, 오히려 **로드맵**이 된다. `/debug`가 어디까지 왔는지 보여준다.

그래도 **그림은 여전히 하나씩** 그린다. 이미지가 유일한 병목이고 수작업이며, 개념
정의의 옳고 그름은 **그림을 그려봐야 드러나기** 때문이다. 한 장에 안 담기면 개념이
둘이라는 신호다 (§4).

목록에 넣기 전에 거른다 — **그릴 수 있는가.** 카드 5종이 전부 이미지를 전제한다.

다만 추상어를 통째로 버리지는 않는다. 시험 어휘는 추상어 비중이 높아(`deadline`이
TSL 18위다) 그것을 다 버리면 어휘가 구체 명사에서 천장을 만난다. **낱말을 못 그리면
예문 한 장면을 그린다** — `deadline`은 빨간 동그라미가 쳐진 달력과 모래시계다.
그림이 뜻을 정의하지 못해도 된다. 뜻은 카드에 한국어로 이미 적혀 있고, 그림은 그
낱말이 사는 자리를 걸어 두는 고리다 (IMAGE_STYLE.md).

이 방식은 **여섯 언어의 예문이 같은 장면을 말할 것**을 요구한다. 그림은 개념당
한 장이기 때문이다. 그 장면이 요소 셋으로 안 담기면 그때는 넣지 않는다.

**목록의 출처는 직접 만든다.** TOEIC도 JLPT도 공식 어휘 목록을 공개하지 않고, 시중의
"필수 1000단어" 류는 교재사의 편집물이다. 레포가 공개이므로 복제하지 않는다. 대신
주제별로 직접 뽑고 `pnpm define`으로 뜻을 사전과 대조한다.

다만 **어느 단어가 그 시험에 나오는지**는 공개 학술 목록으로 확인한다. 영어는
TOEIC Service List(아래 표)가 그 역할을 한다 — 목록을 베껴 콘텐츠를 만드는 게
아니라, 우리가 뽑은 단어가 시험 어휘인지 대조하고 아니면 그 트랙에서 빼는 데 쓴다.

한 개념을 추가하는 절차:

1. 주제 파일(`content/{topic}.json`)에 개념 블록을 쓴다 — `slug`, `meaning_ko`, `category`, `image_prompt`, 언어별 단어
2. `pnpm check` — 검증 (아래)
3. `pnpm prompt <slug>` 출력을 ImageGen에 넣어 1024 PNG를 생성한다
4. 받은 PNG를 `.images/{slug}.png`에 두고 `pnpm image <slug>` — 512 WebP로 변환해 `public/concepts/`에 넣는다
5. **80×80으로 줄여도 알아볼 수 있는지 확인한다** (IMAGE_STYLE.md 검수 체크리스트)
6. 발음을 만들어 `public/audio/{lang}/{slug}.mp3`에 넣는다 ([AUDIO.md](AUDIO.md))
7. 커밋

발음은 없어도 학습이 돌아가므로 나중에 몰아서 해도 된다. 이미지는 3~5단계를 붙여서 한다.

이 절차는 [.claude/skills/add-concept](.claude/skills/add-concept/SKILL.md)에 스킬로도 있다.
규칙이 이 문서와 IMAGE_STYLE·AUDIO 셋에 흩어져 있어 한 번에 꺼내 쓰라고 묶어둔 것이다.
**규칙의 출처는 여전히 이 세 문서다** — 스킬은 절차만 갖는다.

### 발음

규격과 생성 절차는 [AUDIO.md](AUDIO.md)에 있다. 스크립트가 없다 — 손으로 만들어
`public/audio/{lang}/{slug}.mp3`에 넣으면 앱이 알아서 집어간다.

제품 쪽 결정만 여기 적는다. **읽히는 것은 그 언어의 정답 필드다**(`LANG[lang].answer`).
일본어는 읽기(かな)이므로 들리는 소리와 정답이 정확히 같다. 표기(`猫`)를 넘기면 억양이
더 자연스러울 수 있지만 다음톤 한자를 잘못 읽을 위험이 있고, 무엇보다 들리는 것과
정답이 어긋난다.

**발음이 없으면 듣기 카드만 빠진다.** 이미지와 다르다 — 카드 5종이 전부 이미지를 전제하는 것과 달리
발음은 없으면 버튼이 비활성으로 남을 뿐이다.

### 스크립트

업로드가 없으므로 스크립트는 **검증과 변환만** 한다.

| 명령 | 하는 일 |
|---|---|
| `pnpm check` | `content/*.json` 전체 검증. CI에서도 돈다 |
| `pnpm prompt <slug>` | `STYLE_PROMPT + image_prompt` 최종 문구를 출력 |
| `pnpm image <slug>` | `.images/{slug}.png` → 512×512 WebP q80 → `public/concepts/{slug}.webp` |
| `pnpm define <word>` | 사전에서 뜻을 찾는다. 생략하면 콘텐츠 전체의 `meaning_ko`를 대조한다 |
| `pnpm audio` | 발음 현황. `list <lang>`으로 만들 것을, `place <lang> <slug> <파일>`로 받은 파일을 넣는다. 키가 있으면 `make <lang> <n>`이 API로 만든다 ([AUDIO.md](AUDIO.md)) |
| `pnpm audio sync` | 만든 발음을 R2로 올린다. rclone이 바뀐 것만 올린다 ([AUDIO.md](AUDIO.md)) |
| `pnpm levels [파일]` | 시험 등급·순위를 출처에서 채운다 — JLPT · HSK · CEFR · TSL. 추정하지 않고 없으면 비운다 |
| `pnpm tocfl [파일]` | zh의 번체 표기·TOCFL 등급을 여섯 공식 자료에서 채운다. 후보가 갈리면 비우고 터미널에 찍는다 |
| `pnpm romanize [파일]` | ja·zh·ru의 로마자를 규칙으로 채운다. 사람이 쓰지 않는다 |
| `pnpm ipa [파일]` | 영어 발음기호를 CMU 사전에서 채운다. 사전에 없으면 비운다 |
| `pnpm audio manifest` | 발음이 **없는** 자리를 `lib/audio-have.ts`에 적는다. 듣기 카드가 이걸 보고 건너뛴다 (§5) |
| `pnpm coverage` | 시험 목록 대비 우리 위치를 잰다. 표를 문서에 박지 않고 여기서 뽑는다 |

`slug`를 생략하면 아직 결과물이 없는 개념 전체에 대해 돈다.

### 뜻은 기억으로 쓰지 않는다 — `pnpm define`

`meaning_ko`는 사람이 타이핑하는 유일한 의미 정보다. 틀려도 빌드가 깨지지 않고
화면도 멀쩡해서 **눈에 띄지 않는다.** 그래서 사전에서 확인한다.

| 출처 | 주는 것 | 비고 |
|---|---|---|
| Free Dictionary (`dictionaryapi.dev`) | 영어 정의 | 키 없음 |
| 영어 위키낱말사전 | 한국어 번역 후보 | CC BY-SA |
| Jisho (JMdict) | 일본어 읽기·영어 뜻·**JLPT 등급** | 구 출제기준 기반. 아래 한계 참고 |
| complete-hsk-vocabulary | 중국어 **HSK 등급** | MIT. **HSK 3.0**(`new-N`)을 쓴다. 7~9급은 원래 한 묶음이라 `7`로 저장하고 `HSK 7-9`로 적는다 |
| Goethe-Institut Wortliste | 독일어 **CEFR 등급** | 공식 무료 PDF. A1~B1까지만 낸다 |
| TOEIC Service List (TSL 1.2) | 영어 **TOEIC 빈도 순위** | Browne & Culligan, **CC BY-SA 4.0**. 1,250단어로 최근 TOEIC의 98.5%를 덮는다 |
| CC-CEDICT | 중국어 **예문 병음** | **CC BY-SA 4.0**. 낱말 단위 최장 일치로 끊는다 — 글자 단위로 찍으면 다음자에서 틀린다 |

**로마자는 사람이 쓰지 않는다.** 일본어 예문은 규칙상 かな로만 쓰므로 표를 태우면
헵번식이 결정적으로 나온다. 조사는 앞말에서 떼고 소리도 바꾼다(`は`→`wa`·`へ`→`e`·
`を`→`o`) — 붙여 두면 `ほんを よむ`가 `hono yomu`가 되어 읽히지 않는다. 다만
`しごと`처럼 낱말 끝 글자가 조사와 같으면 잘못 떼므로, **콘텐츠에 있는 일본어 읽기를
사전 삼아** 아는 낱말이면 떼지 않는다.

중국어는 CC-CEDICT의 표제어 순서가 빈도순이 아니어서 그대로 쓰면 틀린다(`上`은
`shang3`이 먼저 나온다). 여러 글자 낱말에서 그 글자가 실제로 어떻게 읽혔는지를 세어
가장 흔한 읽기로 한 글자 표제어를 덮고, 고유명사(대문자) 읽기는 보통 읽기가 있으면
쓰지 않는다. 그래도 안 갈리는 기능어 열아홉 자는 표로 박아 둔다.

**독일어 CEFR은 명사만 채워진다.** (`action.json`·`quality.json`이 0인 이유다.) 괴테 목록은 PDF라 표제어와 예문이 한 덩어리로
붙어 나온다. 대문자로 시작하는 낱말만 긁어 독일어 명사를 골라내는데, 동사·형용사는
소문자여서 함께 걸리지 않는다. 소문자까지 긁으면 예문 속 낱말이 전부 들어와 B1
동사가 A1으로 찍힌다 — 정확도를 잃느니 비워 둔다.

**JLPT 등급은 절반쯤만 채워진다.** 2010년 개편 이후 JLPT는 공식 어휘 목록을
내지 않아, JMdict의 태그는 그 이전 출제기준을 따른다. 그 목록은 일반 어휘라
전문어와 파생 복합어가 빠져 있다 — `説明`은 N4인데 `説明書`는 없고, `実験`은
N3인데 `実験室`은 없다. 외래어도 `パン`·`パソコン`은 있고 `ホチキス`·`プリンター`는
없다.

그래서 사무 어휘가 많은 파일일수록 비어 보인다(office 18/53). 조회해 보면
35개 중 34개가 **표제어는 있는데 등급만 없다** — 우리 표기가 틀린 게 아니다.
등급을 얻자고 `ノートパソコン`을 `パソコン`(N4)으로 바꾸지 않는다. 노트북이
아니라 컴퓨터가 된다.

TSL은 CC BY-SA 4.0이므로 출처를 밝힌다 — Browne, C., Culligan, B. (2013).
*The TOEIC Service List*. www.newgeneralservicelist.com

유럽 세 언어 중 **독일어와 러시아어만** CEFR 등급이 붙는다. 스페인어는 Instituto
Cervantes의 PCIC가 있으나 평탄한 목록이 아니고 판권이 걸려 있으며, 프랑스어는 공개된
기계 판독 목록을 찾지 못했다. 둘을 맞추려고 지어내지 않는다 — 없으면 그 카드에 레벨
줄이 안 나온다.

**러시아어는 ros-edu.ru에서 받는다.** ТРКИ 어휘 최소치를 A1~B2 네 등급으로 싣고,
목록 화면이 쓰는 `POST /380`이 그대로 JSON을 준다. robots.txt가 막는 것은
`/assets/...`뿐이라 PCIC를 거른 그 기준을 통과한다. 한 낱말에 등급이 여러 개
달리면(`level_ids`) **가장 낮은 것**을 쓴다 — 그 낱말을 처음 만나는 때다.
목록은 강세를 결합 문자로 얹으므로 뗀 표기끼리 맞춘다. `ё`는 `е`로 접지 않는다 —
`все`와 `всё`가 다른 낱말이다.

**표기가 같아도 뜻이 다르면 비운다.** 목록 대조는 표기로 하므로 동형어가 다른 뜻의
등급을 물려받는다 — `Bank`(우리는 벤치, 목록은 은행), `Karte`(지도/카드)가 그랬다.
등급이 그럴듯해 보여도 근거가 된 낱말이 다르면 근거가 없는 것이다.

**레벨은 조회로 채우고 비면 비운 채로 둔다.** 시험 등급은 지어낼 수 없다 —
JMdict의 JLPT 태그에는 가타카나 외래어가 빠져 있고, HSK 표에도 사무 어휘 상당수가
없다. 값이 없으면 카드에 레벨 줄이 아예 나오지 않는다 (§5).

**`pnpm check`와 분리한다.** check는 CI에서도 돌고 오프라인이어야 하는데 이쪽은 남의
서버에 의존한다. 콘텐츠를 넣을 때 사람이 한 번 돌려보는 자리다.

대조 결과가 어긋나도 **실패가 아니다.** 사전이 넓게 잡거나(`hedge → 울타리`) 표기가
다를 뿐일 수 있다(`주전자` / `주전자(酒煎子)`). 이미지가 가리키는 것과 맞는지 보고 정한다.

> 네이버 사전은 쓰지 않는다. `robots.txt`가 ClaudeBot을 비롯한 자동 수집을 명시적으로
> 막고 있고 공개 API도 없다. 사람이 직접 열어 보는 것은 별개다.

### 번체·TOCFL 등급은 여섯 자료를 겹친다 — `pnpm tocfl`

중국어 `zh.term`은 간체로 고정한다(§4). 대만 TOCFL을 준비하려면 번체가 따로
필요한데, 어느 공식 자료도 그림 명사(체크인 카운터·구명조끼)까지 다 신지
않는다 — 그래서 여섯을 겹친다.

| 출처 | 규모 | 주는 것 |
|---|---|---|
| Unihan `kTraditionalVariant` | 6,475자 | 글자 하나의 간체→번체 후보. Unicode License |
| 華語八千詞表 (TOCFL) | 7,517어 | 표제어가 번체 · **TOCFL 7등급**(準備1·準備2·L1~L5) · 병음 |
| 三等七級詞語表 (國教院) | 14,452어 | 표제어가 번체 · 병음. 후보 검증용 |
| 國語辭典 簡編本 (교육부) | 44,398어 | 후보 검증용. **CC BY-ND** — 표제어 조회만 하고 釋義·예문은 옮기지 않는다 |
| 國語辭典 重編修訂本 (교육부) | 163,921어 | 후보가 갈릴 때 최장 일치로 가른다 |
| 兩岸常用詞語對照表 (교육부) | 863쌍 | 번체화가 아니라 대만에서 **다른 낱말**을 쓰는 자리(土豆→馬鈴薯) |

**후보를 좁히기만 한다. 추정하지 않는다.** Unihan으로 글자마다 번체 후보를
만들고(发→發/髮), ① 공식 표제어에 정확히 하나만 있으면 쓰고 ② 후보가
애초에 하나뿐이면 쓰고 ③ 重編 표제어로 나눠 떨어지는 후보가 하나면 쓰고
④ 重編 최장 일치 점수가 유일하게 가장 높은 후보가 있으면 쓴다. 그래도
갈리면 **비운다** — `台/臺/檯/颱`, `发/發/髮`처럼 사람이 골라야 하는 자리를
터미널에 후보째로 찍는다.

**`scene`(표현 덱, 문장)은 건너뛴다.** TOCFL 등급은 낱말 시험이라 문장
전체에는 안 붙고, 글자마다 후보를 곱하면 문장 길이만큼 부풀어 `了` 하나가
문장마다 `了/瞭` "미확정"을 찍어내며 진짜 갈리는 자리를 파묻는다.

낱말(scene 제외) 대다수가 자동으로 갈리고, **여섯 자료로도 안 갈리는
자리가 남는다** — `台/臺/檯/颱`, `发/髮`, `干/乾/幹`처럼 글자 하나가 뜻에
따라 나뉘는 자리다. 이건 사람이 중국어를 몰라도 된다 — 重編國語辭典의
표제어 釋義(뜻풀이)를 찾아 확인하면 풀린다(스크립트의 `MANUAL` 표에
근거를 남긴다). TOCFL 등급은 八千詞表에서만 붙인다 — 國教院·簡編本·重編은
후보 검증용이지 TOCFL 시험 자체의 어휘표가 아니라서, 섞으면 "TOCFL 등급"
이라는 이름이 거짓말이 된다.

### 점검 화면 — `/debug`

`pnpm dev` 중에만 열리는 개발 화면이다. `pnpm check`가 터미널에서 하는 일을
표로 보여주되 **발음을 실제로 들어볼 수 있다**는 점이 다르다. 개념을 하나씩
손으로 늘리는 워크플로에서 "무엇이 아직 없는지"를 눈으로 확인하는 자리다.

- **한 줄 = 개념 하나 × 언어 하나.** 이미지 · 읽기 · 뜻 · 품사 · 예문 · 발음의 유무를
  한 줄로 늘어놓는다. 언어가 늘면 줄이 늘 뿐 표 모양은 그대로다.
- 언어로 거를 수 있다. **숫자는 걸러진 것만 센다** — 영어만 볼 때 일본어 결손이
  섞여 있으면 읽을 수가 없다. 필터는 화면에서만 도는 상태다. 정적 export라 쿼리
  파라미터를 서버가 읽을 수 없고, 새로고침해 유지할 값도 아니다.
- 파일 유무는 **서버에서 fs로 직접 본다.** 존재뿐 아니라 크기까지 보므로
  0바이트로 남은 실패작이 "없음"으로 잡힌다.
- 발음이 있는 줄에만 재생 버튼이 뜬다. 한 번에 하나만 울린다.
- 프로덕션 빌드에서는 `notFound()`가 먼저 걸린다. `out/debug.html` 파일 자체는
  생기지만 내용은 404 페이지다 — 목록이 새지 않는다.

**표는 둘이고 탭으로 갈아 끼운다 — `단어`와 `상식`.** 세는 단위가 달라서다.
단어 표는 개념 × 트랙이 한 줄이고, 상식 표는 **노트 하나가 한 줄**이다. 한 화면에
겹쳐 놓으면 어느 숫자가 무엇의 숫자인지 흐려진다 — 피드의 덱 탭과 같은 이유다 (§3).

- **상식 표는 "다음에 뭘 캘까"를 위한 화면이다.** 문항마다 `source`에 원본 노트
  이름이 박혀 있으므로(§4) 그것을 세면 노트별 밀도가 나온다. **적게 캔 노트가 위로
  온다** — 많이 캔 노트는 볼 일이 없다.
- 한 문항도 안 나온 노트는 `안 캠`으로 뜬다. 이건 문항이 아니라 **옆 레포의 파일
  목록**에서 오므로 `../brain/notes`(또는 `BRAIN_NOTES`)를 찾을 수 있을 때만 나온다.
  못 찾아도 화면은 돈다 — 캔 노트의 문항 수는 문항 자체에서 나오기 때문이다.
- 숨기지 않고 **떼었다 붙인다.** 단어 표는 보이는 줄만 그리느라 스크롤러의 실제
  높이를 재는데, CSS로 숨기면 그 높이가 0이 되어 아무 줄도 안 그려진다.

**세 번째 탭 `의심 문항`은 오답 품질을 본다** (`lib/trivia-audit.ts`). 낱말 카드는
오답을 풀에서 자동으로 뽑지만(§5) 상식은 손으로 적어서 "내용을 몰라도 답이 보이는"
문항이 섞인다. 1,000문항을 눈으로 다시 읽을 수 없으니 기계가 의심할 자리를 골라 준다.

- **진도 데이터로는 못 잡는다.** 정답률로 거르려면 사람이 많이 풀어야 하는데 진도는
  브라우저마다 갈린 localStorage에 있고(§6), 문항을 쓴 사람은 답을 이미 안다.
  아래 네 신호는 문항 하나만 놓고도 보인다.
- **길이** — 정답이 오답 최댓값의 3배 이상이면서 12자 이상 길거나 짧을 때. 문턱은
  실측으로 잡았다: 1.6배·6자로 두면 23%가 걸려 문체 자체가 걸리는 셈이고, 3배·12자면
  4%가 남아 눈으로 봐도 정답이 튄다.
- **소거** — "차이가 없다"·"규칙이 없다"처럼 내용을 몰라도 지워지는 보기가 둘 이상.
  남는 것이 둘뿐이라 사실상 2지선다다.
- **괄호** — 정답에만 괄호 주석이 붙었다. 친절함이 표지가 된다.
- **중복** — 같은 언어에 같은 물음이 둘.
- **오류가 아니라 후보다.** 정답이 길다고 틀린 문항은 아니라 `pnpm check`는 이걸로
  실패하지 않는다. 사람이 다시 읽어 "괜찮다"고 넘기는 줄이 섞여 있는 것이 정상이다.
- **넘기기로 한 건수는 기준선에 박는다** (`scripts/check.ts`의
  `TRIVIA_SUSPECT_BASELINE`). 넘으면 새로 들어온 문항이 걸린 것이라 경고하고, 밑돌면
  기준선을 낮추라고 알린다. 늘 켜져 있는 경고는 아무도 안 보기 때문이다.
- **괄호는 규칙으로 더 못 좁힌다.** 남은 건은 전부 `没(有)`·`母 (はは)`처럼 괄호가
  표기의 일부라 뗄 수 없는 자리인데, 좁힘 후보를 옛 문항에 돌려 재 보면 진짜 표지였던
  `남성만 (der → den)`이 같이 빠진다 — 원어 괄호가 한쪽에서는 표지이고 다른 쪽에서는
  표기라 모양이 같다. 그래서 신호는 그대로 두고 기준선으로 재운다.

`pnpm check`의 검증 항목:

- 개념 slug 중복
- 개념 slug가 `^[a-z0-9-]+$` 위반
- `category` 누락 — 오답 보기 규칙이 여기 의존한다
- 필수 필드 누락 (`slug`, `meaning_ko`, `image_prompt`, 각 언어의 `term`)
- **`LANG[lang].answer` 필드 누락** — 일본어인데 `reading`이 없으면 출제 불가
- **`category`별 개념 수가 4개 미만** — 오답 보기 3개를 못 뽑는다. 경고로 알린다

`.images/`는 **gitignore**한다. 1024 PNG는 개당 약 883KB이고 재생성 때마다 git 히스토리에
영구히 쌓이기 때문이다. 레포에 들어가는 것은 변환 결과물(개당 평균 5.7KB)뿐이며, 프롬프트가
`content/*.json`에 남아 있으므로 원본이 필요하면 다시 생성한다.

오디오는 변환 단계가 없다. TTS가 이미 최종 포맷으로 주므로 받은 파일이 곧 배포본이다.

---

## 8. 기술 스택

| 영역 | 선택 | 비고 |
|---|---|---|
| 프레임워크 | Next.js App Router | |
| 언어 | TypeScript | |
| 스타일 | Tailwind CSS | 토큰은 CSS 변수로 정의하고 Tailwind에서 참조 |
| 캐러셀 | 없음 — CSS `scroll-snap` | 휠·터치·관성·키보드를 브라우저가 준다. 잠금은 DOM으로 건다 |
| 학습 스케줄 | `ts-fsrs` | FSRS v6. MIT, 의존성 0 |
| 아이콘 | `lucide-react` | 발음 버튼 하나(`audio-lines`)에 쓴다. ISC |
| 컴포넌트 | shadcn/ui (Base UI) — `dropdown-menu` 하나 | 언어 선택에만 쓴다. 색은 브랜드 토큰으로 갈아끼웠고 다크 변형은 지웠다 (§2) |
| 데이터 | `content/{topic}.json` | 빌드 시점 import. 파일은 주제고 트랙은 언어로 고른다 (§4). DB 없음 |
| 정적 자산 | `public/` | 이미지·발음 모두 저장소에. CDN으로 옮기는 스위치는 `NEXT_PUBLIC_AUDIO_BASE` |
| 상태 | React 기본 | 진도·언어 설정은 `localStorage` 래퍼 훅 하나 |
| 이미지 변환 | `sharp` (devDependency) | `pnpm image` 스크립트 전용. 런타임에 안 들어간다 |
| PWA | manifest + 아이콘 | 서비스워커 없음. `app/icon.svg` 하나에서 `pnpm icons`가 나머지를 만든다 |
| 배포 | Vercel (정적) | `output: 'export'` → `out/`. 서버가 필요 없다 |
| 개발 서버 | `pnpm dev` → **5757** | Next의 기본값 3000은 다른 프로젝트와 부딪힌다. 포트를 스크립트에 박아 두면 어느 창에서 켜도 같은 주소다 |

런타임 의존성은 `next` · `react` · `ts-fsrs` · `lucide-react` · `sonner`에 shadcn이 딸고 온
`@base-ui/react` · `clsx` · `tailwind-merge`가 더해진 여덟이다. API 키는 런타임에 없다 —
환경변수는 발음 주소(`NEXT_PUBLIC_AUDIO_BASE`) 하나뿐이고, 그것도 빌드 때 박힌다.

shadcn은 같은 컴포넌트를 Base UI · React Aria · Radix 세 가지로 낸다. **Base UI**를
쓴다. 어느 것을 받을지는 `components.json`의 `style`이 정한다 — `base-vega`처럼
`base-` 로 시작하는 값이어야 하고, `new-york` 같은 옛 이름을 두면 Radix가 딸려 온다.

> 오래 미뤄둔 결정이었다. 처음에는 매칭되는 것이 `Button` 하나뿐이라 도입할 이유가
> 없었다. 언어 드롭다운이 생기면서 **포커스 관리·Esc·바깥 클릭·ARIA**라는, 직접 짜면
> 틀리기 쉬운 것이 한꺼번에 필요해져 그때 들였다. 설정 화면과 단어장(§11)이 오면
> 나머지가 여기서 나온다.

> embla를 쓰려던 것은 **좌우** 캐러셀을 전제로 정한 선택이었다. 세로 피드로 바꾼 뒤에는
> CSS `scroll-snap`이 휠·터치·관성·키보드를 공짜로 주고, 잠금은 어차피 DOM 미마운트라
> embla가 있으나 없으나 같다. 데스크톱 휠에 별도 플러그인이 필요하다는 점도 걸렸다.
> 실제 기기에서 스냅 몸감이 거칠면 그때 `Feed` 내부만 갈아끼운다.

### 렌더링

- `content/*.json`을 **빌드 시점에 import**한다. 전 언어 단어가 그대로 번들에 들어가고,
  클라이언트가 현재 언어 하나만 골라 쓴다. 언어가 URL에 없어도 정적 생성을 잃지 않는다.
- 페이지 전체가 **완전 정적**이다. `revalidate`도 서버 조회도 없다.
  `output: 'export'`로 이 사실을 강제한다 — 빌드 결과가 `out/` 폴더 하나이고
  정적 호스트 어디에나 올린다. 서버 기능을 쓰려는 순간 빌드가 깨지므로
  "서버가 없다"는 결정이 문서가 아니라 빌드로 지켜진다.
- 피드는 클라이언트 컴포넌트다. 진입 시 전체 개념을 메모리에 올려 굴린다.
  개념 1,000개여도 텍스트만 수백 KB 수준이라 문제되지 않는다.
- 이미지는 `next/image`로 `public/concepts/{slug}.webp`를 가리킨다.
  외부 도메인이 없으므로 `remotePatterns` 설정이 필요 없다.
  URL이 개념 slug만으로 결정되므로 언어를 바꿔도 캐시가 유지된다.
- **이미지 최적화는 끈다**(`images.unoptimized`). 파이프라인이 이미 512×512 q80
  최종본(개당 5KB)을 만들므로 최적화기가 더 줄일 게 없다. 덤으로 `/_next/image`가
  400을 받고도 캐시된 옛 이미지를 계속 그리던 문제가 사라진다.
- **카드 마운트는 지연한다.** 미응답 퀴즈 다음 카드는 아예 렌더하지 않는다 —
  잠금을 CSS가 아니라 DOM 구조로 거는 것이 훨씬 견고하다.

---

## 9. 아직 정하지 않은 것

구현 전에 답이 필요하다.

| # | 항목 | 지금 상태 |
|---|---|---|
| 1 | **단어 목록** | **없다.** 미리 만들지 않고 하나씩 지명해 추가한다 (§7) |
| 2 | 예약 거리 수치 (3 / 8 / 20 / 2장) | 제안치. 실사용 감각으로 조정 |
| 3 | 쿨다운 K와 신선도 부스트 감쇠율 | 미정 |
| 4 | 체류 시간을 `Rating.Hard`로 쓸지 | 보류. 지금은 2진 채점 |
| 5 | 피드 소진 — 개념을 다 소개하면 신규 15%를 뽑을 데가 없다 | 미정 |
| 6 | ~~플레이스홀더 이미지 실물~~ | 인라인 SVG로 해결 (§4) |
| 7 | 반복 오답 쌍을 confusion 카드로 잡을지 | v2 후보 |
| ~~8~~ | ~~한자 빈칸 카드~~ | 해결. 자형이 닮은 한자 표를 만들어 HSK도 세 칸을 다 쓴다 (§5) |

문서 곳곳의 "60개"는 규모 감각을 위한 예시일 뿐 목표 수치가 아니다.

---

## 10. 구현 순서

각 단계는 그 자체로 확인 가능해야 한다.
**콘텐츠가 한 개도 없어도 1~6단계가 진행된다** — 개념은 그 사이에 하나씩 늘어난다.

1. **셸** — Next.js·Tailwind 설치, 토큰 정의(§brand-spec), 전체화면 피드 컨테이너
2. **타입과 검증** — `Concept` / `Word` / `LangStrategy` 타입, `content/` 로더, `pnpm check`
3. **카드 5종** — 소개 / 재인 / 듣기 / 문맥 / 단서 회상 렌더러와 채점. 손으로 쓴 개념 2~3개로 굴린다
4. **캐러셀** — CSS `scroll-snap`, 지연 마운트, 퀴즈 잠금
5. **학습 엔진** — `ts-fsrs` 연결, rung 이동, 예약 큐, 가중 샘플러 + 단위 테스트
6. **이미지 파이프라인** — `pnpm prompt` / `pnpm image`, 플레이스홀더 제작
7. **발음** — 오디오 제작([AUDIO.md](AUDIO.md)), 버튼 활성화 규칙
8. **콘텐츠** — 개념을 하나씩 추가한다. 여기서부터는 끝이 없다
9. **마무리** — PWA manifest, 아이콘, 메타데이터, 배포

DB 단계가 없다. 3단계에서 필요한 개념 2~3개는 이미 있는
`banana` · `cat` · `bread` · `clock` 이미지를 그대로 쓴다.

동작 참고 구현은 [lingo-feed-prototype.html](lingo-feed-prototype.html)에 있다.

---

## 11. 확장 경로

지금 만들지 않지만 구조가 막지 않아야 하는 것들.

| 시점 | 추가 | 영향 |
|---|---|---|
| 2번째 언어 | 각 개념의 `words`에 언어 블록 + `LANG` 항목 추가 | **이미지 재생성 없음.** 발음만 새로 생성 |
| 한자 트랙 | `LANG.ja` 옆에 `answer: 'term'` 전략 추가 | 같은 개념을 두 트랙으로 각각 외운다. 진도 키만 분리 |
| 단어장 화면 | 라우트 추가 + 상단 탭 | 낱말별 진도가 여기로 들어온다. 헤더는 총합 한 줄까지만 맡는다 (§3) |
| 설정 화면 | 라우트 추가 | 음소거, 학습 대상 전환 |
| 카드 타입 추가 | rung 표에 행 추가 | 렌더러 컴포넌트만 추가. **두 번 해 봤다** — 문맥 카드는 rung을 하나 늘렸고(진도 v1→v2 마이그레이션), 듣기 카드는 rung을 늘리지 않고 재인 칸에서 번갈아 낸다 (§5) |
| **DB 도입** | 로그인·진도 동기화가 필요해지는 시점 | 콘텐츠는 계속 레포에 둔 채 진도만 서버로 올려도 된다. §4 타입이 그대로 스키마가 된다 |
| 로그인 | Auth + `learning_records` | 기존 `localStorage` 진도를 최초 로그인 시 1회 마이그레이션 |
| 예문 여러 개 | **스키마는 열어 뒀다** — `Word.examples` 배열이 있고 문맥 카드가 회차로 돌려 쓴다. 남은 것은 콘텐츠다 | 전량은 18,340문장이라 못 쓴다. 어느 낱말이 자주 반복되는지는 **미리 못 고른다** — 신규 유입이 무작위이고 그다음은 그 사람이 틀린 것이 자주 나오기 때문이다(엔진 시뮬레이션). 겨냥할 수 있는 것은 **풀이 작은 트랙**뿐이다 — TOEIC은 594개라 같은 낱말을 다섯 배 자주 만난다 |
| 문법·회화 | `grammar_points`, `conversations` + 단어 연결 | 단어가 허브가 되는 구조 |
| 앱 내 이미지 생성 | OpenAI Images API | 프롬프트를 개념에 분리 보관하므로 그대로 재사용 |
