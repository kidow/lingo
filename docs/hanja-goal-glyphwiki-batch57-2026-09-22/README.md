# 慌13획 적용 — 원본 곡선·위쪽 갈고리

국내 사전의 전체13획 방향·누적·완성 자형과117개 중간 재생 상태를 공개 원본7건으로 재현한 도형에 대조했다. 忄를 왼쪽 점→오른쪽 점→세로 순서로 바꾸고, 원본13개 도형을 모두 보존했다. 꺾임은 원본 이차곡선으로 재생한다. 마지막 위쪽 갈고리는 고정 kagecd.js의 a2==5 분기가 생성한 별도 도형을 가로 뒤에 위로 드러낸다. 새 경로나 좌표를 만들지 않았다.

아래 삐침의 긴 왼쪽 뻗음과 둥근 가로 끝은 검토한 순서·방향·접속을 유지하는 서체 차이로 기록했다. 국내 사전 교차검토이며 시험 주관기관의 공식 승인은 아니다. 공통 렌더러와 기존1.5배 속도는 변경하지 않았다.

원본7건·엔진8파일 재조회/해시/도형/호출 재현, 신규3개와 전체888개 한자 테스트(180파일), TypeScript 포함 격리 빌드7페이지가 통과했다. 격리 빌드와17개 파일·공개 배포파일3개가 일치한다. 모바일430×932에서 소개/쓰기 자동재생·다시 재생·13/13완료, 정적 SVG 오버레이 비중첩, 필기0→1→0, 캔버스 드래그 시 시트 위치 유지, 콘솔 오류·경고0건을 확인했다.

신규 **慌1자·13획**, 실제 적용 **4,981/5,978자(83.3%)**, 미적용 **997자**. 남은 급수는1급65·2급1·특급II293·특급638이다. 다음은 전체 원본5건을 확보한 **竪13획**이다. 별도 탐색한 嚆·描의 기본-k 별칭은 초두3획이므로, 다른 전체 자형을 찾기 전 적용 후보로 승인하지 않았다(next-source-leads.json).

검증 재현: `node docs/hanja-goal-glyphwiki-batch57-2026-09-22/verify.mjs . --sources --engine`. 검토 기록은 progressive-review.json, 검사·UI 결과는 validation.json, 공개 배포 자산과 고지는 public/hanja-strokes/glyphwiki/614c.json 및 NOTICE-614c.md다. 비공개 사전 그래픽은 RAM에서만 대조했다. 임시 탭·서버·격리 체크아웃은 정리했다.
