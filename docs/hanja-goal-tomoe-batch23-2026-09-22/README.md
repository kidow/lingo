# 饐 Tomoe 21획 적용 / 隴·鏘 보류 — 2026-09-22

기준 HEAD: `bbb2b7d9be6623770cf741b5712336790c74ffb9`. 전체 3자·59획의 개별 경로·방향·누적 자형과 완성형을 국내 사전과 대조했다. 국내 사전 그래픽은 RAM에서만 확인했으며 저장하지 않았다. 사전 교차검토이며 한국어문회 인증을 뜻하지 않는다.

| 한자 | 급수 | 배정/재생 획수 | 결과 |
|---|---|---:|---|
| 隴 | 특급 | 19 | 보류 |
| 饐 | 특급 | 21 | 적용 |
| 鏘 | 특급 | 19 | 보류 |

## 판단과 변경

- **饐**: 食의 4번째 원본 세로획을 7번째로 옮겨 `[1,2,3,5,6,7,4,8,9,10–21]` 순서로 재생한다. 기존 normalizeMedians로 균일 정규화하고 선 두께 **2.6**을 사용했다. 보정된 전체21획·누적 상태·완성형을 다시 대조했다. 豆19/20획 하단은21획 바닥에 닿고, 상단은18획과 분리된다. 이전 KanjiVG 후보의 보류를 무시한 것이 아니라, 다른 Tomoe 원본에서 해결 가능성을 확인했다.
- **隴**: 阜2획 끝을3획에 연결하려면 선폭4.9 이상, 立5획 시작을阜1획과 분리하려면4.6375 미만이 필요하다. 동일 두께로 양쪽 조건을 만족할 수 없다.
- **鏘**: 金7획을5획에 연결하려면 선폭3.5857 이상이 필요하지만, 오른쪽15획 끝을14획과 분리하려면2.5771 미만이어야 한다. 원본爿9/10획도 교환이 필요하다. 전체 보정 승인은 하지 않았다.

거리 계산은 시각 검토를 보조하며 단독 승인 기준이 아니다. `width-probe.py`가 공개 원본을 해시 확인 후 수치를 재현한다. 새 좌표 추가·획 분할·병합·반전은 하지 않았다. 자세한 판단과 해제 조건은 findings.json이다.

## 출처와 재현

Tomoe `l4u/tomoe@9d054a1f490368e0d45e0aff6430ce3598ab92c5`의 `data/handwriting-ja.xml`을 사용한다. 전체 원문 SHA-256은 `6f30a4f42f24dc611f1a3e4d7a220c5009bb0323f237f01f50e3be3dfefb11f8`이다. 상위 README는 KANJIDIC2를 제외한 LGPL을 명시하고 COPYING은 LGPL2.1이다. 이번 데이터는 KANJIDIC2를 사용하지 않는다.

원래 饐 레코드를 변경 없이 추출한 `public/hanja-strokes/tomoe/9950.xml`, 원본 README·COPYING, 변경 고지를 함께 배포한다. `build.mjs`는 이 편집 가능한 원본과 검토 기록에서 런타임 JSON을 재현한다. 출처·순서·두께·검토 해시·라이선스를 런타임 모듈에 고정했다. 재현 원본과 라이선스의 실제 정적 빌드 포함도 확인했다.

## 검증

- `node docs/hanja-goal-tomoe-batch23-2026-09-22/verify.mjs`: 3자 원문·원본 좌표·정규화 해시, 전체59획 검토 기록, 승인/보류 등록 검증 통과.
- `python3 docs/hanja-goal-tomoe-batch23-2026-09-22/width-probe.py`: 저장한 거리 결과와 일치.
- 별도 검증 워크트리: 한자 테스트 **833/833**, TypeScript 포함 Next.js 프로덕션 빌드 통과. 검증 대상14파일이 원 작업 트리와 바이트 단위로 일치했다. 처음 전체 테스트에서 신규 묶음을 누락한 합산 검사를 발견해 Tomoe 묶음을 포함한 뒤 재검증했다.
- 실제 프로덕션 번들에서 饐 소개 자동재생11/21→21/21, 다시 재생1/21→21/21 확인. 쓰기 바텀시트 진입 자동1/21→21/21, 다시 재생1/21→21/21 확인.
- 별도 테스트 origin에 饐만 신규인 진도 fixture를 RAM에서 주입했다. 애플리케이션 번들은 변경하지 않았으며 사용자 origin의 진도는 건드리지 않았다.
- 쓰기 캔버스 SVG1개, 선 두께2.6, touch-action:none. 실제 아래 방향 드래그 후0→1획 입력, 취소·지우기 활성화. 캔버스 위치(46,162.8046875), 크기338×338, 상위 scrollTop 배열[0,0,0,126,0,0]이 입력 전후 그대로 유지됐다.
- 앱 경고·오류 로그0개. 기존 UI·속도 코드는 변경하지 않았다. 검증 탭·뷰포트·RAM서버51854–51857·임시 워크트리를 정리했다.

## 진행 현황

이번 **1자·21획 적용**, **2자·38획 보류**. 전체 **4,964/5,978자(83.0%)**, **1,014자 미적용**. 특급은 **690/1,328자(52.0%)**,638자 남음. 다른 급수는 변동 없다. 급수별 집계는 coverage.json.

다음은 Tomoe 미검토 후보5자 중 **駸17·褊14·諞16, 총47획**이다. 이번 커밋은 `git log -1 --format=%H -- docs/hanja-goal-tomoe-batch23-2026-09-22/findings.json`으로 확인한다. 푸시·배포는 하지 않았다.
