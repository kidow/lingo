# 특급 隰·阨 24획 적용 / 饐 21획 보류

2026-09-22. 시작 HEAD `90192c0e`. 국내 사전 교차검토이며 시험기관의 공식 인증이 아니다.

## 검토 결과

- **隰 17획 적용**: 096b0 원순서를 유지했다. 국내 사전의 정확한 隰 자형에서도 阝 하단은 열려 있으므로 다른 글자의 연결을 추정하지 않았다. 日 내부 가로획은 기본판이 맞고 Kaisho는 짧다. 幺 두 묶음과 灬의 전체 방향을 대조했다. 굵기 5는 日 아래와 幺 위가 붙어 원본 굵기 3을 100/109로 축소한 **300/109**를 선택했다. 이 굵기로 누적 17개와 완성형을 다시 확인했다.
- **阨 7획 적용**: 09628 원순서와 기본 굵기 **5**를 유지했다. 국내 사전도 阝 하단이 열려 있다. 厂의 가로·왼쪽 내림과 오른쪽 굽힘·갈고리를 포함한 7획 전체와 누적 자형을 확인했다.
- **饐 21획 보류**: 食 순열 `1,2,3,5,6,7,4,8,9,10,…,21`은 맞지만 굵기 5에서 豆의 두 점이 口 밑변에 붙는다. 원본 비례 굵기에서는 분리되지만 두 점 아래 끝이 마지막 받침과 모두 연결되지 않는다. Kaisho는 食 내부 6획만 달라 이 문제를 해결하지 않는다. 정확한 자형의 공개 경로나 독립적인 끝점 보정 근거가 있어야 재검토한다.

기본 경로 **45획**, 굵기 5의 정규화 누적 상태 **45개**, 원본 굵기의 隰 누적 상태 **17개**를 직접 대조했다. 대체판은 다른 6획을 선택적으로 검토했으며 전체 승인으로 세지 않는다. 饐 원본 굵기는 완성형 비교만 했고 전체 누적 재검토로 세지 않는다. 구체적인 판단은 `findings.json`에 있다.

KanjiVG 고정 리비전 `422b5538595676da918c288a4230cb5e22a1ee7e`, CC-BY-SA-3.0, © Ulrich Apel and contributors. 국내 사전 세 SVG는 크기·해시를 확인하고 RAM에서만 비교했다. 사전 SVG·HTML·스크린샷은 저장하지 않았다. 공개 후보 경로는 `candidates.json`, 정규화 후보는 `normalized.json`, 승인 데이터 재현은 `build.mjs`에 있다. 획 분할·병합·점 이동·부수 합성은 없다.

## 검증

- Node 전체 한자 테스트 **813개 통과**, 타입 검사 통과. 첫 테스트 호출은 실행 환경의 Bun이 node:test를 거부하여 종료했다. Node로 수정한 전체 실행이 통과했으며 실패 실행을 통과로 세지 않았다.
- 분리 체크아웃에서 오프라인 설치·프로덕션 빌드 통과. 제품/검토 파일 **9개**가 본 작업 트리와 바이트 단위로 같았다.
- 실제 HanjaCard 학습 자동재생: 隰 **0→17**, 阨 **1→7**. 두 글자 모두 다시 재생 **1→마지막 획**을 확인했다.
- 쓰기 진입 자동재생과 다시 재생도 각 **1→17**, **1→7**. 완료 시 일시정지 대신 재생 버튼으로 바뀌었다.
- 쓰기 캔버스 SVG 각 1개, 굵기 隰 **2.75229px**, 阨 **5px**, 정적 오버레이 중복 없음, `touch-action: none`. 각 글자에서 실제 드래그로 **1획 입력** 후 캔버스·시트 사각형과 스크롤이 입력 직전과 같았다.
- 앱 콘솔 오류 **0**; 무관한 확장 프로그램 오류 4개는 분리했다. 데스크톱 Chrome이며 실제 모바일 기기 검증은 하지 않았다.
- 플레이어·재생 속도·바텀시트 코드는 변경하지 않았다. 전용 탭과 RAM 서버 3개·정적 서버를 닫고 임시 체크아웃을 제거했다.

## 진행 상태와 다음 작업

전체 **4,959/5,978자(83.0%)** 적용, **1,019자** 미적용.
특급 **687/1,328자(51.7%)**, 641자 남았다. 전체 급수 집계는 `coverage.json`에 있다.

다음은 **鏘 19획·髢 13획**의 전체 대조와 **爨 배정 29획/사전 30획**의 변형 확인이다. `next-candidates.json`은 후보 메타데이터이며 승인 기록이 아니다.
이번 커밋은 `git log -1 --format=%H -- public/hanja-strokes/dictionary-reviewed-special-kanjivg-batch8.json`으로 조회한다.
