# GlyphWiki 34차 — 菩12획 적용

고정된 전체 글자 원본5개와 국내 사전의 전체12획 방향·누적12상태·완성형을 대조했다. 한국어문회 공식 승인으로 표시하지 않는다.

- 원시13도형에서 명시적 그룹11+12의 같은 좌표 모서리만 연결해12획으로 재현한다. 풀머리3·4순서를 교환하며 경로점·방향은 그대로 보존한다.
- 선폭4에서는 내부 곡선이 윗가로획에 닿는 차이를 발견해 기각했다. 동일 경로의 선폭2로 전체를 재검토하여 시작점의 분리, 왼쪽 점 끝의 분리, 오른쪽 곡선 끝의 접속을 확인했다.
- 혼합 곡선/모서리 처리는 명시적 옵션에서만 허용한다. 검토된 곡선 코드와 연결 수직선만 통과하며 임의 곡선 병합·갈고리·역방향을 거부한다. 결과는 원본 중심선 애니메이션이며 서체 윤곽의 완전 복제는 아니다.
- 원본5개 재조회 일치, 한자 테스트 **855개/170파일**, 별도 연결 검사6개, 타입 검사·프로덕션 빌드 통과.
- 실제430×932 화면에서 소개·쓰기 자동/다시 재생, 필기·취소0→1→0, SVG1개·선폭2, 시트/캔버스 위치 유지, 콘솔 오류/경고0건 확인. batch34 임시 자원 정리 완료.

신규 **1자·12획**, 전체 **4,971/5,978자(83.2%)**, 미적용 **1,007자**: 1급75·2급1·특급II293·특급638.

재현: `node docs/hanja-goal-glyphwiki-batch34-2026-09-22/verify.mjs --sources`. geometry-semantics.json은 경로·선폭 의미, findings.json은 전체 검토, validation.json은 실제 검증 기록이다.

다음 **葺13획**은 전체 글자 원본5개를 확보했다. 풀머리 간격을 보존하는 선폭1.5 후보의 모든 획·누적·완성 자형을 검토하고, 통과할 때만 연결 수직선 처리 및 런타임을 추가한다. 아직 승인하지 않았다.
