# GlyphWiki 31차 — 葵 13획 적용

국내 사전과 공개 원본 전체 경로를 대조했다. 기본 k형의 중간 획 접속과 대체 원본의 풀머리 간격 차이를 확인하고, 같은 葵의 두 고정 원본으로 검토한 조합을 만들었다.

- 기본 `u8475-var-001@1`의 명시된 배치와 좌표를 사용한다. 마지막 획만 같은 글자의 `u8475-var-003@2` 원시 도형14에서 가져왔다. 원본 그대로의 단일 폰트라고 표시하지 않는다.
- 기본 도형5+6은 같은 원본에서 연속하며 끝점과 시작점이 정확히 같고, KAGE의 수평 연결0/2→오른쪽 위 모서리22/7로 선언되어 있다. 임의로 연결선을 추가하지 않는다.
- 그룹 순열 `[1,2,4,3,5,9,6,8,7,13,10,11,12]`, 200→100 정규화, 선폭4.5. 원본점 수정·방향 반전·부수 추정 조립 없음.
- 전체13획 방향, 누적13상태, 완성 자형 검토 통과. 국내 그래픽은 RAM에서만 대조했다.
- 원본6개 리비전 재조회 일치. 한자 테스트 **849개/168파일**, 타입 검사와 프로덕션 빌드 통과. 기존 변환기 기본 동작을 보존하고 명시적 선택과 연결 조건으로 새 형태를 제한했다.
- 실제 모바일 화면에서 소개·쓰기 자동/다시 재생 **1→13**, 필기·취소 **0→1→0**, SVG1개·선폭4.5, 캔버스/시트 위치와 scrollTop 유지, 콘솔 오류/경고0건을 확인했다. 임시 자원 정리 완료.

신규 **1자·13획**, 전체 **4,969/5,978자(83.1%)**, 미적용 **1,009자**. 남은 급수: 1급77, 2급1, 특급II293, 특급638.

재현: `node docs/hanja-goal-glyphwiki-batch31-2026-09-22/verify.mjs --sources`.
검토 근거는 findings.json·curve-semantics.json, 실제 검증은 validation.json, 후속 후보는 next.json에 있다. 국내 사전 교차검토이며 한국어문회 공식 승인으로 표시하지 않는다.
