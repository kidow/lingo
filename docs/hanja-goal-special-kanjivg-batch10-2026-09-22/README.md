# 특급 駸·褊·諞 3자·47획 검토 — 보류

2026-09-22. 시작 HEAD `a13a3293`. 국내 사전과 공개 경로를 교차검토했으며 이번 배치의 신규 적용은 **0자**다.

## 결과

- **駸 17획**: 馬의 국내 순서는 기본 후보 `1,2,4,5,3,6,7,…,17`이다. Vt6 변형도 원순서 그대로는 맞지 않는다. 원본 전체 획의 방향을 대조하고, 이 순열을 100/109로 정규화해 굵기 5와 원본 비례 굵기 300/109의 완성형을 비교했다. 又의 마지막 17획 시작점이 16획의 가로 부분 아래에 떨어져 있다. 굵기 5에서는 옆 점 14와도 붙는 반면, 원본 굵기는 16획과의 틈을 해결하지 못한다. 두 변형은 해당 경로가 동일하다. 독립 근거 없이 시작점을 옮기지 않았다.
- **褊 14획**: 국내 衤 첫 획은 거의 수직으로 내려가지만 후보는 오른쪽으로 기운 점이다. 戶 첫 획 6은 국내에서 오른쪽→왼쪽이고 왼쪽 내림 9가 그 높이에서 시작한다. 후보는 왼쪽→오른쪽의 분리된 가로획이며 9획은 더 아래의 7획에서 시작한다. Kaisho는 冊 내부 12획만 달라 해결하지 못한다.
- **諞 16획**: 국내 言 첫 획은 가로획인데 후보는 오른쪽 아래 점이다. 戶의 8·11획도 褊과 같은 방향·연결 차이가 있다. Kaisho는 冊 내부 14획만 다르며 문제 경로를 바꾸지 않는다.

기본 후보 **47획 전체**의 순서·방향·경로를 직접 대조했다. 駸의 정규화는 검토 후보이며 승인 경로가 아니다. 완성형 차이가 남아 정규화 누적 전체를 승인하지 않았다. 보류 해제 조건과 변경 경로 인덱스는 `findings.json`에 있다.

공개 경로는 KanjiVG 리비전 `422b5538595676da918c288a4230cb5e22a1ee7e`, CC-BY-SA-3.0, © Ulrich Apel and contributors. 후보 6개와 출처·해시·원본 굵기를 `candidates.json`에 보존했다. 국내 사전 그래픽은 해시 대조 후 RAM에서만 열었으며 SVG·HTML·스크린샷을 저장하지 않았다.

## 검증

`node docs/hanja-goal-special-kanjivg-batch10-2026-09-22/verify.mjs` 통과.
47획 검토 범위, 현재 런타임 미등록, 후보 경로 수, 대체판 경로 동일성, 駸 순열·정규화 재현을 검사했다.

제품 코드·런타임 데이터는 바꾸지 않았다. 따라서 이번 보고서 배치에서 빌드나 UI 검증을 재실행했다고 주장하지 않는다. 직전 적용 배치 `a13a3293`에서 테스트 817개·타입·분리 빌드·실제 재생과 캔버스 입력을 검증했다. 이번 전용 탭과 RAM 서버 3개는 정리했다.

## 진행과 다음 작업

현재 **4,960/5,978자(83.0%)** 적용, **1,018자** 미적용으로 유지된다.
특급 **688/1,328자(51.8%)**, 640자 남았다. 급수별 집계는 `coverage.json`에 있다.

다음은 **餔 16·鐶 21획**의 전체 검토와 **敝 배정 12/사전 11획** 차이 조사다. 후보 메타데이터는 `next-candidates.json`에 있다. 이후 釁 25획을 검토하고 남은 후보 목록을 갱신한다.
이번 커밋은 `git log -1 --format=%H -- docs/hanja-goal-special-kanjivg-batch10-2026-09-22/findings.json`으로 조회한다.
