# 17차 — HanziVG의 기존 경로 차이 검증

2026-09-22, 기준 커밋 `21aae54b`.

국내 획수가 일치했던 HanziVG **25자·45파일**을 기존 고정 KanjiVG와 비교했다. 공백과 숫자 표기 차이를 제외한 경로 명령·좌표 토큰이 **42파일에서 동일**했다.

남은 **麑 3파일**도 새 경로가 아니었다. 기본·Kaisho의 14/15획이 교환되어 있었고, 예전 `KaishoHzFst` 파일은 현재 알려진 `KaishoHzLst`와 대응했다. 같은 이름의 URL이 404여서 이미 이전 검토에 등장한 실제 대안 파일로 대조했다. 세 파일 모두 전체 19개 경로가 기존 경로의 순열이며, 보류 원인인 **3·4·7·9·12획은 그대로**다.

[7차 시각 검토](../hanja-goal-special-kanjivg-batch7-2026-09-22/findings.json)의 둘레 획 접속·부품 간 간격 문제는 해제되지 않았다. 새 국내 시각 검토를 수행했다고 세지 않으며 신규 적용 **0자**, **4,963/5,978자(83.0%) 적용 · 1,015자 미적용**이다.

## 다음 자료

[Tegaki 원 저장소](https://github.com/tegaki/tegaki)의 고정 리비전 `7a74e442c4130cccc226a7e7c2b683ac94c0cccb`을 조회했다. 압축된 Tomoe 일본어 자료에 현재 미적용 **173자**의 파일이 있다. 이는 파일명 기준 후보 수이며, 국내 획수·방향·자형 일치나 사용 허가를 뜻하지 않는다.

다음 순서는 **코퍼스별 출처·이용 조건 확인 → 압축 XML의 획 경계·좌표 분석 → 국내 획수 대조 → 전체 자형 검토 → 통과분 런타임 적용**이다. 코드의 GPL 및 모델 LGPL 고지를 자료 전체의 라이선스로 단정하지 않는다. 이번 커밋에는 해당 자료의 경로를 복사하지 않았다.

## 검증

`node docs/hanja-goal-fallback-diff-batch17-2026-09-22/verify.mjs`는 두 저장소의 고정 리비전에서 실제 경로를 다시 받아 45파일 비교, 麑 세 파일의 19획 순열, 기존 보류 원인 경로의 동일성, 미등록 상태를 검증한다. API 결과는 메모리에서 처리하며 원본 그래픽은 저장하지 않는다. 런타임 변경이 없어 앱 빌드·UI 검증을 반복하지 않았다.
