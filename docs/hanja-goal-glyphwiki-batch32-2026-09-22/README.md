# GlyphWiki 32차 — 苔 5획 경계 보류

국내9획 중5·6획을 공개 원본 도형5·6·7과 RAM에서 대조했다. 5획은 왼쪽 아래로 내려온 뒤 오른쪽으로 이어지는 한 획인데, `u82d4-k@9`의 실제 경로는 서로 다른 점에서 끊긴다.

- 종속 원본 `u53f0-04@4`: 첫 곡선 끝(52,113), 다음 곡선 시작(27,115). 배치·정규화 뒤에도 (26,57.065)와 (13.5,58.075)로 다르다.
- 페이지에 연결된 `u82d4-var-001@3`·`u82d4-t@2`도 각각 경계 (27,69)→(28,74), (33,61)→(28,63)가 일치하지 않는다. 두 대안에는 미검증 stretch도 있어 완성형을 승인하지 않았다.
- 단순 결합·경로 절단·새 연결점 생성은 하지 않았다. 전체9획 승인이나 보정 누적 검토 완료로 집계하지 않는다.
- 재조회한 고정 기록13개가 일치했다. `node docs/hanja-goal-glyphwiki-batch32-2026-09-22/verify.mjs --sources`로 원본 경계·리비전·미적용 상태를 검사한다.

신규0자, 苔1자·9획 보류. 전체 **4,969/5,978자(83.1%)**, **1,009자 미적용** 유지. 런타임 변경이 없으므로 직전849개 검사·빌드를 반복하지 않았다. 그래픽·HTML·스크린샷은 저장하지 않았고 검토 탭·RAM서버를 정리했다.

다음 **菖12획**: 원본14도형에 있는 두 日의 수평→수직 모서리가 출처에 명시된 필기 연결인지 먼저 검증하고, 통과하면 전체 획을 대조한다.
