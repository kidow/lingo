# 迅 7획 — 마지막 꺾임 경로 보류

`u8fc5`와 대체 완성 글자 `u8fc5-ue0102`를 국내 사전의 전체7획·방향·누적·완성 자형과 대조했다. 기본 원본의 한 점·직선 자형 차이는 대체 원본에서 해소되지만, 마지막7획의 두 중심경로가 연결되지 않는다.

- 앞 이차곡선 끝: (55,154). 뒤 삼차곡선 시작: (53,152).
- 완성 외곽선이 겹쳐 보여도 연속 필기 경로가 되는 것은 아니다. 연결선 추가, 임의 자르기·좌표 이동은 하지 않는다.
- 고정2016년 공개 백업의 전체SHA256·정확한 의존 레코드와 고정 엔진8파일 재현을 재검증했다. 두 점을 사용하는 다른 확보 별칭도 같은 `u8fb6` 원본을 공유하므로 독립 해결책으로 세지 않는다.
- 실행: `node docs/hanja-goal-glyphwiki-batch75-2026-09-22/verify.mjs --sources --engine` 통과. 런타임 변경이 없어 제품 빌드를 반복하지 않았다.

추가 적용0자. 누적4,992/5,978자(83.5%), 미적용986자. 남은 수: 1급65,2급1,특급II288,특급632. 나머지 급수는 모두 적용됐다.

다음 후보 扤(특급6획)은 원래 완성 글자 `u6264`의3개 의존 레코드와 엔진 재현을 확보했다. k별칭 부재를 부수 조립으로 메우지 않고 실제 존재하는 완성 원본을 사용한다. 전체 시각 대조는 다음 단계다.

비공개 사전 그래픽은 RAM에서만 사용했고 비교 탭·서버는 종료했다. 공개 원본 RAM조회 서버는 다음 작업에 유지한다.
